# Vercel デプロイ構成マップ（VERCEL_DEPLOYMENT_MAP）

作成日: 2026-09-17 / 状態: **調査のみ。削除・設定変更・本番変更は一切していない**
調査方法: GitHub の Vercel bot コメント／commit status ＋ **`/api/health` と `/version.json` への読み取り専用 GET**

> ⚠️ **Vercel API へのアクセスは持っていません。** 管理画面でしか分からない項目
> （環境変数の中身、カスタムドメイン、プロジェクトの作成日、アクセスログ）は
> **「未確認」と明記**しています。推測は「推定」と区別して書いています。

---

## 0. 結論（先に）

| | 結論 | 確度 |
|---|---|---|
| **スタッフが使っている本番** | **`dashboard`** ただ1つ | **実測（高）** |
| `dashboard-7x9k` / `dashboard-sae7` | **環境変数が空で実質機能しない。旧環境または誤作成の可能性が高い** | **実測（高）** |
| main への push | **3プロジェクトすべてが自動デプロイされる** | **実測（高）** |
| 環境変数の共有 | **共有していない。3つとも中身が違う** | **実測（高）** |
| KV / GAS の参照 | **`dashboard` だけが参照している。他2つはどこにも繋がっていない** | **実測（高）** |
| **PR #380 / #381 の実影響** | **`dashboard` のみ**（他2つは壊れようがない） | **実測（高）** |

---

## 1. 各 Vercel Project の用途

| Project | Project ID | 用途（判定） | 根拠 |
|---|---|---|---|
| **`dashboard`** | `prj_OeLHrV9th1Aj0taAllaDdFeOfUYP` | **本番。スタッフが実際に使っている** | 環境変数が完全に揃っている（Square 91アカウント・KV・GAS）。ログイン画面を正常に配信 |
| `dashboard-7x9k` | `prj_kAKwhUazzPtk6vepwqwleq0Px1ic` | **不明（実質空）。旧環境/誤作成の可能性** | 環境変数が**すべて未設定**。Square 0・KV なし・GAS なし |
| `dashboard-sae7` | `prj_hKdEekwlKrItd1lgf0Xass3Ti6ML` | **不明（実質空）。旧環境/誤作成の可能性** | `SQUARE_TOKENS` は存在するが**アカウント数 0**（空配列か壊れた値）。KV なし・GAS なし |

Team: `team_qIQ9nrrMuhCw90Li5VgqE2Y8`（`daikiwakabayashs-projects`）
3つとも `rootDirectory: null`＝**リポジトリ全体をビルド**する同一構成。

> **なぜ3つあるのかはコードからは分かりません。** `api/health.js` の末尾に
> `// redeploy trigger: git再接続後の同期確認 (67cdccd)` というコメントが残っており、
> **過去にGit連携をやり直した形跡**があります。そのときに作られた残骸である可能性がありますが、**推測です**。

---

## 2. Production Domain

`/api/health` への GET で実測した結果です。

| ドメイン | HTTP | 状態 |
|---|:--:|---|
| **`dashboard-daikiwakabayashs-projects.vercel.app`** | **200** | **本番。完全に設定済み** |
| `dashboard-7x9k-daikiwakabayashs-projects.vercel.app` | 302 | Vercel SSO 保護（チームメンバーのみ） |
| `dashboard-sae7-daikiwakabayashs-projects.vercel.app` | 302 | 同上 |
| `dashboard-7x9k.vercel.app` | 200 | 保護なしで見えるが**環境変数が空** |
| `dashboard-sae7.vercel.app` | 200 | 同上（ほぼ空） |

> ⚠️ **カスタムドメイン（独自ドメイン）が設定されているかは確認できていません。**
> スタッフが `naoru-...` のような独自ドメインで使っている可能性があります。
> → **Vercel 管理画面の各プロジェクト「Domains」で確認してください。**（§8 の確認事項 ①）

---

## 3. 現在利用中か

| Project | 配信 | 環境変数 | 判定 |
|---|:--:|:--:|---|
| **`dashboard`** | ✅ ログイン画面を正常配信 | ✅ 完全 | **利用中** |
| `dashboard-7x9k` | ✅ 配信はされる | ❌ 空 | **利用できない**（ログインもデータ取得も全て失敗する） |
| `dashboard-sae7` | ✅ 配信はされる | ❌ ほぼ空 | **利用できない**（同上） |

環境変数が無いと、`DASHBOARD_PASSWORD` も `SETTLEMENT_GAS_URL` も無いため
**ログインそのものが成立しません**。画面は出ても業務には使えない状態です。

---

## 4. 同じ Environment Variables を使っているか

**いいえ。3つとも中身が違います。**（`/api/health` の実測値）

| 変数の存在 | `dashboard` | `dashboard-7x9k` | `dashboard-sae7` |
|---|:--:|:--:|:--:|
| `SQUARE_ACCESS_TOKEN` | ✅ | ❌ | ❌ |
| `SQUARE_TOKENS` | ✅ | ❌ | ⚠️ 存在するが**0件** |
| Square アカウント数 | **91** | 0 | 0 |
| `SQUARE_LOCATION_ID` | ✅ | ❌ | ❌ |
| `SQUARE_ENVIRONMENT` | ✅ | ❌ | ❌ |
| KV（`KV_REST_API_*` 等） | ✅ | ❌ | ❌ |
| Supabase | ❌ | ❌ | ❌ |
| GAS（`PLAN_GAS_URL`/`SETTLEMENT_GAS_URL`） | ✅ | ❌ | ❌ |

> Preview と Production で同じ値が共有されている点は別途確認済みです
> （`INTEGRATION_PLAN.md` §E）。そのため Command Center では
> **KVキーを環境ごとに分離**しています（`feature/cc-foundation`）。

---

## 5. 同じ KV / Supabase / GAS を参照しているか

**いいえ。`dashboard` だけが参照しています。**

```
dashboard        ──→ KV（有効）──→ naoru:* の全データ
                 ──→ GAS（有効）─→ オーナー設定・返金明細書
dashboard-7x9k   ──→ どこにも繋がっていない
dashboard-sae7   ──→ どこにも繋がっていない
```

- **Supabase はどのプロジェクトでも未設定**（`supabase: false`）。
  → 現在の保存先は **KV が唯一の実経路**です。
- `dashboard` は GAS も有効ですが、これは `SETTLEMENT_GAS_URL`（返金明細書用）が
  設定されているためです。**plan-store の保存先としては KV が優先**され、GAS には到達しません。

```js
// api/plan-store.js — 保存先の優先順位（実装）
async function blobGet(key, hasKV, hasSB, gas) {
  if (hasKV) return await kvGet(key);     // ← 本番はここで必ず返る
  if (hasSB) return await sbGet(key);
  return (await gasCall(...)).value;      // ← 到達しない
}
```

→ **本番の実際の保存経路は KV です。**（§7 のリスク評価の前提）

⚠️ ただし KV に障害が起きて `KV_REST_API_URL` が外された場合のみ、GAS 経路に落ちます。
その場合の同時更新の安全性は §7-2 に記載。

---

## 6. main への push でどのプロジェクトが自動デプロイされるか

**3つすべてです。**

根拠: 現在3プロジェクトが配信しているビルドIDが**同じ分（同時刻）に生成**されています。

| Project | 配信中の build |
|---|---|
| `dashboard` | `1789612485137` |
| `dashboard-7x9k` | `1789612485055` |
| `dashboard-sae7` | `1789612485590` |

※ `build` は `scripts/precompile.mjs` が生成する `Date.now()`。差は 535ms＝**同一 push を3プロジェクトが並行ビルド**した証拠です。

PR の Vercel bot コメントでも、1つのPRに対して3プロジェクトが Preview を作っています。

**つまり `main` への1回のマージが、3つの Production Deployment を同時に更新します。**

---

## 7. 現在スタッフが使っている本番環境

**`dashboard`** です。

| 判断材料 | 結果 |
|---|---|
| Square 91アカウント分のトークンが入っている | 実運用の規模と一致 |
| KV が有効（全社データの保存先） | チャット・掲示板・手当がここに入っている |
| GAS が有効（オーナー設定・返金明細書） | ログインの土台 |
| ログイン画面を正常に配信 | 実際に使える |
| 他2つは環境変数が無くログインすら成立しない | 消去法でも `dashboard` |

⚠️ **カスタムドメイン経由で使われている可能性**は残ります（§2）。
ただしどのドメインを使っていても、**裏側は `dashboard` プロジェクト**である可能性が高いです。

---

## 8. 旧環境または不要環境があるか

**`dashboard-7x9k` と `dashboard-sae7` は不要である可能性が高い**ですが、**断定できません。**

| 判断 | 根拠 |
|---|---|
| 不要の可能性が高い | 環境変数が空＝業務で使えない。`dashboard` と完全に重複した構成 |
| **断定できない理由** | ①カスタムドメインが割り当てられている可能性 ②誰かが検証用に使っている可能性 ③Vercelのアクセスログを見られない |

### 確認していただきたいこと（Vercel 管理画面）

| # | 確認項目 | 場所 |
|---|---|---|
| ① | 各プロジェクトに**カスタムドメイン**が付いていないか | Project → Settings → Domains |
| ② | `dashboard-7x9k` / `dashboard-sae7` に**直近30日のアクセスがあるか** | Project → Analytics（または Logs） |
| ③ | スタッフに案内している URL はどれか | 運用側で確認 |
| ④ | 3プロジェクトの**作成日**（どれが最初か） | Project → Settings → General |

### 不要と確認できた場合の推奨（**今は実行しません**）

1. **まず Git 連携だけを外す**（Settings → Git → Disconnect）
   → デプロイが止まるだけで、プロジェクトもドメインも残るので**すぐ元に戻せます**
2. 1〜2週間様子を見て、誰も困らないことを確認
3. その後に削除を判断

⚠️ **いきなり削除しないでください。** ドメインが割り当たっていた場合、
そのURLを使っている人が即座にアクセスできなくなります。

---

## 9. PR #380 / #381 を main へマージした場合の影響範囲

### 9-1. デプロイの影響

```
main へ merge
   ├─→ dashboard        本番が更新される  ← 実質的な影響はここだけ
   ├─→ dashboard-7x9k   ビルドは走るが、環境変数が無いので動作は変わらない（元から使えない）
   └─→ dashboard-sae7   同上
```

### 9-2. `dashboard`（本番）への影響

| 項目 | 影響 |
|---|---|
| **画面・操作・見た目** | **変化なし**（UIは1行も変更していません） |
| **保存先** | KV。**キー名は既存のまま**。新しいキーが2つ増えるだけ |
| **新しく作られるキー** | `naoru:board:reads:v1`（#380）／`naoru:allowance:log:v1`・`naoru:allowance:prod:v1`（#381） |
| **既存キーへの影響** | `naoru:board:v1` は**投稿の書き込み時のみ**更新（旧 `reads` は保持）／`naoru:allowance:v1` は提出の写しとして更新（旧 `productivity` は保持） |
| **既存データ** | **移行不要**。読み出し時に新旧をマージするので、切り替え直後から従来どおり見えます |
| **環境変数** | **変更不要**。追加も削除もありません |
| **Serverless Function 数** | **変わりません**（11/12 のまま） |
| **Supabase** | **未使用のまま**（本番では設定されていません） |
| **他の機能** | チャット・売上・マーケ・返金明細書・サンクスギフトには**一切触れていません** |

### 9-3. マージ直後に起きること

| タイミング | 掲示板（#380） | 手当（#381） |
|---|---|---|
| 直後 | 既読が新キーへ書かれ始める。**旧既読も引き続き読まれる** | 提出がログへ積まれ始める。**旧提出も引き続き読まれる** |
| 画面を開いたままの人 | 古いJSのまま動くが、**サーバー側で既読が投稿に触れないので安全**（§9-4） | フロント無変更のため**そのまま動く** |
| 数分後 | 全員が新しいJSを読み込む（`version.json` の自動更新） | 同左 |

### 9-4. 「画面を開いたままの古いクライアント」について

**掲示板の最大の損失経路は、クライアントの版に関係なく塞がれます。**
既読が投稿に触れないのは**サーバー側の変更**だからです。古いJSが `expectedVersion` を
送らなくても、そのリクエストの `read` 処理は新しいコードで動きます。

残る細いリスクは §10 に記載します。

### 9-5. 戻し方

| 手段 | 所要 | 結果 |
|---|---|---|
| Vercel Instant Rollback | 数十秒 | 3プロジェクトとも直前のデプロイへ。**投稿・提出は失われません** |
| `git revert` | 数分 | 同上 |

#380・#381 とも「旧形式へも書き続ける」設計なので、**戻したあとも旧コードが全データを読めます**。

---

## 10. マージ前に把握しておくべき残存リスク

`DATA_CONSISTENCY_PLAN.md` および本番の実経路（KV）を前提とした評価です。

| # | リスク | 本番（KV）での影響 | 評価 |
|---|---|---|---|
| 1 | 掲示板の投稿更新が真のCASではない（確認→書込の間に窓がある） | 投稿・コメントが**同じ数百ミリ秒**に重なった場合のみ。旧来の「閲覧のたび」に比べ桁違いに小さい | 🟡 残る |
| 2 | 手当の非KV経路（Supabase/GAS）は追記が原子的でない | **本番では到達しない**（KVが有効なため） | 🟢 通常経路では安全 |
| 3 | 掲示板ログ上限5000件 | 溢れた分は旧blob側の写しに残る | 🟢 小 |
| 4 | Lua（原子的追記）の実機動作 | テストは模擬実装。**Preview の実KVでの確認が望ましい** | 🟡 要確認 |

**#1 と #2 は「通常経路では安全だが、条件が揃うと残るリスク」です。**
詳細な評価と追加修正の要否は、この後の分析で判断します。

---

## 11. 判断のまとめ

| 問い | 答え |
|---|---|
| #380 / #381 をマージして安全か | **`dashboard` 以外への影響はありません。** `dashboard` についてもUI・データ・環境変数は変わりません |
| 3プロジェクトが同時更新されることは問題か | **問題になりません**（他2つは元から動いていないため） |
| マージ前に必須の確認 | **§8 の①②**（カスタムドメインとアクセスの有無）。これは私からは確認できません |
| 削除・整理 | **今はしないでください。** Git連携を外す → 様子見 → 削除、の順を推奨 |
