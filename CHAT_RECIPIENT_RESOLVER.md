# CHAT_RECIPIENT_RESOLVER — 宛先解決の仕様

**ロジック**: `lib/chat-recipients.js` / **テスト**: `tests/chat-recipients.test.js`（40件）

---

## 1. 入口と出口

```js
resolveRecipients(spec, { dir, actor, onBehalfOf, can })
```

| 引数 | 中身 |
|---|---|
| `spec` | 宛先指定（AIまたはUIが組み立てた**構造**。自由文ではない） |
| `dir.shops` | `[{ id, name, area }]` — 全社の店舗ディレクトリ |
| `dir.staff` | `[{ id, name, shop, role }]` — 全社のスタッフディレクトリ |
| `actor` | 実際に実行した主体（AIなら `source:'agent'`） |
| `onBehalfOf` | 依頼した人間。**権限判定はこちら** |
| `can` | 認可関数の注入（省略時は保守的な `fallbackCan`） |

出口は `{ status, shops, staffIds, ambiguities, unresolved, outOfScope, recipientCount, requiresBulkConfirm, … }`。

---

## 2. 名前の正規化（`normName`）

真実は `store_id` / `staff_id`。名前は**入口の検索語**にすぎない。
ただし人は名前でしか話さないので、表記ゆれを吸収する。

| 入力 | 正規化後 | 吸収するもの |
|---|---|---|
| `恵比寿店` | `恵比寿` | 接尾辞（店 / 店舗 / サロン / 整骨院） |
| `恵比寿 店舗` | `恵比寿` | 空白（半角・全角） |
| `ＮＡＯＲＵ` | `naoru` | 全角英数 → 半角、大文字 → 小文字 |
| `NAORU・恵比寿` | `naoru恵比寿` | 記号（・, 、- _ / （） 【】 []） |

`null` / `undefined` / 数値でも落ちない。

---

## 3. 候補の絞り込み（`matchCandidates`）

```
完全一致が1件でもあれば → 完全一致のみを返す（部分一致は見ない）
完全一致が0件         → 部分一致をすべて返す
```

**なぜ完全一致を優先するか**: 「恵比寿」と「恵比寿西口」が両方あるとき、
「恵比寿」と言った人は恵比寿のことを言っている。ここで両方返すと、実在する
店舗名をそのまま言ったのに毎回確認を求められて使い物にならない。

---

## 4. 3分類（`resolveNames`）

| 候補数 | 分類 | 返すもの |
|---|---|---|
| 1 | `resolved` | `{ query, item }` |
| 0 | `unresolved` | `{ query }` — **近い名前へ寄せない** |
| 2+ | `ambiguous` | `{ query, candidates[最大10件] }` — 人に選ばせる |

### 推測しない、の具体例

| 依頼 | 状況 | 動き |
|---|---|---|
| 「田中さんに」 | 田中太郎・田中花子の2人 | 止める。2人を候補として出す |
| 「田中太郎さんに」 | 1人 | 確定 |
| 「山田三郎さんに」 | 該当なし | 止める。「山田」で始まる人に寄せない |
| 「東京の店舗に」 | エリア辞書に東京あり | 確定（3店舗） |
| 「北海道の店舗に」 | 該当なし | 止める |

---

## 5. 集合の組み立て順

```
1. scope='all' なら全店を入れる
2. spec.shops を名前解決して足す
3. spec.areas を解決して足す
4. exclude.shops を解決して引く          ← 曖昧なら止める
5. spec.staff を名前解決して足す
6. spec.roles を解決して足す
7. 確定した店舗の在籍者を足す
8. exclude.staff を解決して引く          ← 曖昧なら止める
9. 権限の外にある宛先を落として outOfScope に記録
10. 操作そのものの可否（全社一斉の役割・宛先数上限）を判定
```

### 除外が曖昧なときに止める理由

「恵比寿は除いて」で2件該当した場合、除外できないまま送ると**送ってはいけない先に届く**。
宛先の曖昧さ（届かない）より、除外の曖昧さ（届いてはいけない先に届く）のほうが危険。
どちらも止めるが、優先度の説明としてこちらを明記しておく。

---

## 6. 権限の当て方（2段構え）

混ぜてはいけない2つの判定を、意図的に分けてある。

| 段 | 判定 | 使う操作 | 結果 |
|---|---|---|---|
| ① 宛先ごと | その店舗へ届くか | 常に `chat.send` | 落ちたら `outOfScope` に記録 |
| ② 操作全体 | 一斉送信そのものを行えるか | `chat.send` / `broadcast` / `broadcast_all` | 落ちたら `denied` |

**混ぜるとどうなるか（実際に起きた不具合）**: スタッフが2店舗を指定したとき、
①で `chat.broadcast` を使って判定すると、スタッフは broadcast の役割を持たないため
**両方の店舗が消え**、「自店にも送れない」という説明のつかない結果になった。
①を `chat.send` に固定することで、「自店は届く / 他店は権限外」と正しく分かれる。

---

## 7. `status` の決まり方

```
① 操作全体が拒否            → denied
② 曖昧 or 不明が1件でもある  → needs_confirmation
③ 権限外が混ざっている
     └ 残りが0件            → denied（確認する余地がない）
     └ 残りがある           → needs_confirmation
④ 宛先が0件                 → empty
⑤ それ以外                  → resolved
```

**送信ボタンを出してよいのは `resolved` のときだけ。**

---

## 8. 一括送信の二段確認

```
requiresBulkConfirm = (宛先 ≥ 20名) または (chat.broadcast_all)
```

「全店に送ります」ではなく「**312名に送ります**」と数で見せる。
規模が言葉では伝わらないため。

---

## 9. 壊れた入力への耐性（テスト済み）

| 入力 | 動き |
|---|---|
| `spec = null` | `empty`（落ちない） |
| `ctx = {}` | 落ちない |
| `dir` が空 | 全部 `unresolved` → `needs_confirmation` |
| 未検証の主体 | `denied` または `empty` |
| `spec.shops` に空文字 | 無視される |

---

## 10. 未対応（今後）

| 項目 | 備考 |
|---|---|
| Smart / Dynamic Group | 「入会率が30%未満の店舗」のような**条件で決まる宛先**。宛先が時間で変わるので、送信時点で確定させて監査に残す必要がある |
| 音声 → 文字起こし → spec | 文字起こしの誤変換が宛先に直結する。**確認を必須**にする（`requiresBulkConfirm` を常時ONにする案） |
| 役職の階層（エリア長 → 配下の店舗） | 組織の階層データが未整備。`lib/geo.js` のエリアで代用中 |
| 同名の店舗が別エリアにある場合 | 現状は `ambiguous` で止まる。エリアを添えて確定させる導線が要る |
