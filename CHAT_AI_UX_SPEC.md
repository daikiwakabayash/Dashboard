# CHAT_AI_UX_SPEC — 画面と文言

**状態**: 設計のみ。`index.html` は未変更（巨大ファイルの競合を避けるため別PRで実装）
**前提**: すべて `cc_chat_routing` フラグの下。既定OFF・キルスイッチ対象。

---

## 1. 既存の画面を壊さない

| 既存 | 変更 |
|---|---|
| チャットの入力欄・送信ボタン | **そのまま**。フラグOFFなら今と1ピクセルも変わらない |
| `🤖 AIに質問` ボタン | そのまま（質問回答は既存の `agent:'faq'`） |
| ルーム一覧・未読バッジ | そのまま |

追加するのは「**送信の前に1枚挟まる確認**」だけ。

---

## 2. 依頼から送信までの画面

### 2-1. 依頼（既存の入力欄）

```
┌────────────────────────────────────────────┐
│ 東京の店舗全部に、恵比寿西口は除いて        │
│ 「明日は10時開店」と送っておいて            │
│                          [🤖 AIに頼む] [送信] │
└────────────────────────────────────────────┘
```

`[送信]` は今までどおり、そのルームへ普通に送る。
`[🤖 AIに頼む]` を押したときだけ宛先解決が走る。

### 2-2. 送信前プレビュー（`status === 'resolved'`）

```
┌─ 宛先の確認 ───────────────────────────────┐
│                                            │
│  送信先                                     │
│  ● 店舗 2件（恵比寿、渋谷）                  │
│  ● 28名に届きます                           │
│                                            │
│  除外                                       │
│  ✕ 恵比寿西口                               │
│                                            │
│  本文                                       │
│  ┌──────────────────────────────────────┐  │
│  │ 明日は10時開店                        │  │
│  └──────────────────────────────────────┘  │
│                                            │
│           [キャンセル]  [この宛先で送信]     │
└────────────────────────────────────────────┘
```

**必ず見せるもの**: 店舗名（5件まで＋「ほか」）／**人数の実数**／除外した先。

### 2-3. 二段確認（`requiresBulkConfirm`）

20名以上、または全社一斉のとき、もう1枚挟む。

```
┌─ 確認 ─────────────────────────────────────┐
│                                            │
│   312名 に送信します。                       │
│   送信後は取り消せません。                   │
│                                            │
│           [戻る]     [312名に送信する]       │
└────────────────────────────────────────────┘
```

ボタンの文言に**人数を入れる**。「OK」だと読まずに押される。

### 2-4. 宛先が曖昧（`status === 'needs_confirmation'`）

```
┌─ 宛先を確定してください ────────────────────┐
│                                            │
│  「田中」に当てはまる人が2人います           │
│    ○ 田中 太郎（恵比寿）                    │
│    ○ 田中 花子（渋谷）                      │
│                                            │
│  「北海道」に当てはまる店舗がありません       │
│    → 言い直してください                     │
│                                            │
│                        [やり直す]           │
└────────────────────────────────────────────┘
```

- **送信ボタンを出さない**
- 候補には**所属店舗を添える**（同名の判別は店舗でしかできない）
- 「たぶんこちらでは？」という推測の提案をしない

### 2-5. 権限外が混ざっている

```
┌─ 一部の宛先に送れません ────────────────────┐
│                                            │
│  送れる                                     │
│  ● 恵比寿（あなたの所属店舗）                │
│                                            │
│  送れない                                   │
│  ✕ 渋谷 — この店舗へ送る権限がありません     │
│                                            │
│     [キャンセル]  [恵比寿だけに送信]         │
└────────────────────────────────────────────┘
```

落とした先を**必ず見せる**。黙って恵比寿だけに送ると「渋谷にも送ったつもり」になる。

### 2-6. 権限がない（`status === 'denied'`）

```
┌────────────────────────────────────────────┐
│  全社への一斉送信は本部のみが行えます        │
│  本部に依頼しますか？                        │
│              [閉じる]  [本部に依頼する]      │
└────────────────────────────────────────────┘
```

拒否で終わらせず、**次にできること**を出す。

---

## 3. AIの回答に必ず添えるもの

AIが業務の質問に答えるとき（既存の `agent:'faq'` を含む）、回答の下に出す。

```
────────────────────────────────
📎 根拠: 就業規則FAQ（2026-08-12 更新）
📊 データ: SalonOne 2026年8月実績（本日 05:00 取得）
⚠️ この回答はAIが作成しました。重要な判断は本部に確認してください。
```

| 表示 | 出典 |
|---|---|
| 根拠 | FAQ / ナレッジ資料のタイトルと更新日 |
| データ | SalonOne / Platform の `source` と `freshness` |
| 注意書き | 常時 |

**根拠が出せないときは答えない**（`NEEDS_HQ` で本部へエスカレ）。これは既存の動きを踏襲。

---

## 4. 本部がAIの回答を訂正する

既存の「🤖 この回答をFAQに追加」を拡張する。

```
AIの回答 → […] → 「この回答は間違っている」
         → 正しい回答を入力 → FAQに登録 ＋ 元の回答に訂正マークを付ける
```

**訂正マークは消さない**。間違った回答が残っていること自体を、あとから追えるようにする。

---

## 5. 文言の原則

| ✅ こう書く | ❌ こう書かない | 理由 |
|---|---|---|
| 312名に送信します | 全店に送信します | 規模が伝わらない |
| 「田中」に当てはまる人が2人います | 宛先が曖昧です | 何をすればいいか分からない |
| この店舗へ送る権限がありません | 権限エラー | 誰に相談すればいいか分からない |
| 送信後は取り消せません | よろしいですか？ | 取り返しのつかなさが伝わらない |
| 本部に確認してください | AIは間違えることがあります | 行動にならない |

---

## 6. 予約送信・未読者再送（R4）

### 予約送信
- 送信前プレビューは**同じ**。日時を足すだけ
- **宛先は予約した時点で確定させる**。実行時に再解決しない
  （配属が変わって意図しない人に届くのを防ぐ）
- 予約一覧から取り消せる。実行済みは取り消せないと明記する

### 未読者再送
- 「未読の18名に再送」と**人数を出す**
- 既読の人には**送らない**（同じ通知が2回来ると通知自体が無視されるようになる）
- 再送は元メッセージへの追記ではなく**新しいメッセージ**。監査で区別できるよう
  `channel: 'resend_unread'` を付ける

---

## 7. 実装しないもの

| 項目 | 理由 |
|---|---|
| AIが確認なしで送信 | 取り消せない |
| 「たぶんこの人では？」の提案 | 推測を提示すると押される |
| 送信後の自動取り消し | 既読になったものは取り消せない。できないことをUIで約束しない |
| 宛先の自然文表示のみ（IDを持たない） | 表示と実際の宛先がずれる |
