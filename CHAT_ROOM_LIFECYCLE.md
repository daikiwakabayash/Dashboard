# CHAT_ROOM_LIFECYCLE — ルームの生成・在籍・アーカイブ

**ロジック**: `lib/chat-lifecycle.js` / **テスト**: `tests/chat-lifecycle.test.js`（28件）
既存の `lib/chat.js`（`ensureBaseRooms` / `roomVisibleTo`）は**変更していない**。ここはその上の層。

---

## 1. 原則

| 原則 | 理由 |
|---|---|
| 自動で**入れる**のは安全側 | 見えるようになるだけ。事故にならない |
| 自動で**出す**のは慎重に | 連絡が届かなくなる。気づけない |
| ルームは**消さない**（畳むだけ） | 過去のやり取りは監査の対象 |
| 手で入れた人は自動で外さない | 本部・エリア長が配属外だから外される、を防ぐ |

---

## 2. 店舗ルームの自動生成（`planStoreRooms`）

SalonOne の店舗一覧と現在のルームを突き合わせ、3つの判断を返す。**書き込みはしない**。

| 判断 | 条件 | 動き |
|---|---|---|
| `create` | 店舗一覧にあってルームが無い | 店舗ルームを作る（ID = `store_<店舗名>`） |
| `rename` | ルームはあるが名前が違う | **名前だけ直す**。作り直さない（履歴を切らないため） |
| `archive` | ルームはあるが店舗一覧に無い | 畳む（消さない） |

### 🔴 店舗一覧が空のときは何も畳まない

SalonOne API が失敗して空配列を返したとき、素直に処理すると**全店舗ルームを畳む**。
これは連絡経路の全損なので、`shopList.length === 0` のときは archive を一切出さず
`skippedArchive: true` を返す。テストで固定してある。

---

## 3. 在籍の同期（`syncStoreMembers`）

SalonOne の配属情報から、店舗ルームの在籍者を計算する。

```
members = （その店舗に配属されている人） ∪ （pinnedMembers）
add     = 新しく配属された人
remove  = 配属から外れた人 − pinnedMembers
```

| 仕組み | 目的 |
|---|---|
| `pinnedMembers` | 本部・エリア長など、配属に載らないが入れておきたい人。自動で外さない |
| `autoRemove: false` | 移行期間の安全弁。誰も外さずに「入れるだけ」で運用できる |
| `changed` | 変更が無ければ書き込み自体を行わない（無駄な更新で競合を起こさない） |

**導入時は `autoRemove: false` から始める。** 自動退室は、しばらく `add` だけで運用して
配属データの質を確認してから有効にする。

---

## 4. イベントルーム（`shouldArchiveEventRoom`）

勉強会・飲み会・部活などに、期間限定のルームを作れるようにする（ID = `event_<id>`）。

| 状況 | 動き |
|---|---|
| 終了日 + **7日**（`EVENT_ARCHIVE_GRACE_DAYS`）を過ぎた | 畳む |
| 終了直後 | 畳まない（片付け・振り返りの投稿が入るため） |
| 日付が読めない（「毎週火曜」「未定」） | **畳まない**。読めないものを推測で畳まない |
| ルームごとに `graceDays` を指定 | その日数を優先 |

既存の「勉強会・イベント日程」タブ（`lib/events.js`）の行と紐付ける想定。
日付の解釈は `lib/events.js` の `parseEventDate` に揃える（別PR）。

---

## 5. 畳む・戻す（`archiveRoom` / `unarchiveRoom`）

```js
archiveRoom(room, 'event_ended')
// → { ...room, archived:true, archivedAt:'2026-09-10T…', archivedReason:'event_ended' }
```

- 元のオブジェクトは書き換えない（純粋関数）
- **二重に畳んでも最初の時刻が残る**（いつ畳んだかが監査の対象なので上書きしない）
- `unarchiveRoom` で戻せる

### 畳めないルーム（`canArchive`）

| ルーム | 畳める？ | 理由 |
|---|---|---|
| 全社アナウンス | ❌ | 全社の連絡経路。消えると代わりが無い |
| 店舗ルーム | ❌（手動では） | 店舗一覧の変更でのみ畳む。手で畳むと在籍者が気づけない |
| グループ・イベント | ✅ | 作った人が畳んでよい |

---

## 6. 一覧での扱い（`visibleRooms`）

畳んだルームは既定で隠すが、`includeArchived: true` で出せる。
**検索と監査では常に含める**（過去のやり取りを探せないと畳んだ意味が失われる）。

---

## 7. 実装時の注意（R3 で作業する人向け）

1. `planStoreRooms` の結果は**そのまま書き込まない**。`create` / `rename` は自動でよいが、
   `archive` は最初のうちは**ログだけ**にして、想定どおりか確認してから有効化する。
2. 書き込みは既存の `mutatePosts` と同じ **CAS + リトライ**で行う（`api/plan-store.js`）。
   ルーム一覧は全員が読む共有blobなので、read-modify-write で消える。
3. 同期の実行者は **広い権限のセッション**（root / brand_admin）に限る。
   既存の `ensureRooms` / `setdir` と同じ思想。
