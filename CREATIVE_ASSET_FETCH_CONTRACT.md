# 素材取得契約（①Dashboard → ③生成サービス）draft-1

- 方向: **③が①の素材を取りに来る**（これまでの「①が③の成果物を取りに行く」の逆方向）
- 状態: **契約案。①はまだ実装していません。**
- 起案: ①（2026-09-19）／③が `CREATIVE_REVIEW_FIXED_20260920.md` で提示した要件に対する回答

> ⚠️ **この口を開けるには若林さんの承認が要ります。**
> ①の素材を外部から読める口を新しく開けること、そのための専用鍵を登録することは、
> いずれも「外部公開」と「新しい認証情報の登録」にあたります。
> 契約が固まっても、承認があるまで①は実装も登録もしません。
> 現状は ③の `CREATIVE_JOB_CONTRACT.md` のとおり「実権利・credential・実素材は未受領」で変わりません。

---

## 0. ③の要件と、①の回答

| ③の要件 | ①の回答 |
| --- | --- |
| 専用 read-only credential | ○ `CREATIVE_ASSET_READ_KEY` を新設。既存のどの鍵とも別にする（§2） |
| tenant / store / asset / version をサーバー確認 | ○ ①の台帳で照合。③の申告だけでは通さない（§4） |
| metadata に hash / MIME / bytes / rights / 有効期限 | ○ メタだけ先に返す口を分ける（§3） |
| 固定 origin / path の GET、redirect 拒否 | ○ ①は**リダイレクトを返さない**。③は `redirect: 'error'` で呼ぶ（§5） |
| 画像20MB・動画100MB上限 | ○ そのまま採用。超過は切り詰めず 413（§6） |
| timeout 10秒・429/5xx のみ最大2回再試行 | ○ 採用。4xx は再試行しない（§7） |

---

## 1. 口

固定 origin・固定 path。③は他のURLを組み立てない。

```
GET  {DASHBOARD_ORIGIN}/api/creative-asset?asset_id=<id>&version=<n>&meta=1   … メタだけ
GET  {DASHBOARD_ORIGIN}/api/creative-asset?asset_id=<id>&version=<n>          … 実体
```

`{DASHBOARD_ORIGIN}` は①が③へ別途通知する1つのoriginのみ。
③はこの2本以外を叩かない。クエリは上記3つだけを受け付け、未知のクエリは無視する。

## 2. 認証

```
Authorization: Bearer <CREATIVE_ASSET_READ_KEY>
X-Tenant-Id: <tenant_id>
```

- `CREATIVE_ASSET_READ_KEY` は**この口の読取専用**。32文字以上・ASCII・空白なし。
- ①の共通ログイン、`CC_AGENT_TOKEN`、`CREATIVE_GEN_API_KEY` の**いずれとも別の値**にする。
  この鍵で読めるのは素材の実体とメタだけで、書き込み・一覧・他の `type` には一切到達しない。
- 鍵の受け渡しは**チャットを経由しない**。若林さんが①と③それぞれの環境変数へ直接登録する。
- 失効は①側で環境変数を差し替えるだけで即時。③は401を受けたら**再試行せずに止める**（§7）。
- ①は `hmac.compare_digest` 相当の定数時間比較を使い、**非ASCIIのヘッダで500にしない**
  （③の `creative_job_app.py` で見つけた Medium-1 と同じ壊れ方を①側で作らないため）。

## 3. メタ（`meta=1`）

先にメタだけ取り、③が自分の台帳と照合してから実体を落とす。

```json
{
  "api_version": "creative-asset-1",
  "tenant_id": "naoru",
  "store_id": "s_0001",
  "asset_id": "as_xxxxx",
  "version": 3,
  "content_type": "image/png",
  "bytes": 1048576,
  "sha256": "…64hex…",
  "rights": { "status": "confirmed", "confirmed_by": "…", "confirmed_at": "2026-09-19T…Z",
              "expires_at": "2027-03-31T…Z", "scope": "meta_ad_draft" },
  "kind": "real",
  "data_mode": "live",
  "fetched_at": "2026-09-19T…Z"
}
```

- `sha256` は**平文の**ハッシュ。③はダウンロード後にこれと突き合わせる。
- `rights.status` が `confirmed` 以外の素材は、そもそもこの口から出さない（404ではなく 403 `RIGHTS_UNCONFIRMED`）。
- `expires_at` を過ぎた素材は 410 `RIGHTS_EXPIRED`。③は保持していた複製を削除する。
- ⚠️ `kind: "demo"` と `"real"` を混ぜない。③は `real` を受け取ったら、
  デモ素材と同じ置き場・同じ manifest に入れない。

## 4. 権限の確認（①側）

③の申告は**どれも信用しない**。①は毎回サーバー側で確認する。

1. `CREATIVE_ASSET_READ_KEY` の一致
2. `X-Tenant-Id` が①の台帳の素材の `tenantId` と一致
3. `asset_id` が実在し、`version` が**その版として保存されている**こと
   （最新版を勝手に返さない。版がずれたら 409 `VERSION_MISMATCH`）
4. `rights.status === 'confirmed'` かつ `expires_at` 未経過
5. ①側で素材が停止（承認取消・ACL失効）されていないこと

どれか1つでも欠けたら実体は返さない。**「たぶん大丈夫」で通さない。**

## 5. 実体

- 常に `200`（Range 指定時は `206`）で**バイト列を直接返す**。
- **`301/302/303/307/308` を返さない。** 保存先のURLも署名付きURLも返さない。
  ③は `fetch(..., { redirect: 'error' })` 相当で呼び、リダイレクトを受けたら失敗として扱う。
- ヘッダ:
  - `Content-Type`: メタの `content_type` と同じ
  - `ETag`: `"<sha256>"`。③が `If-None-Match` を付ければ `304`
  - `Accept-Ranges: bytes` / `Content-Range`（206のとき）
  - `Cache-Control: private, no-store`（共有キャッシュに載せない）
  - `X-Content-Type-Options: nosniff`
- ①は保存時に暗号化している素材を、この口で復号して返す。
  **平文を保存先に置き直すことはしない。**

## 6. 大きさ

| 種別 | 上限（案） |
| --- | --- |
| 画像 | 20 MB |
| 動画 | 100 MB |

- 超えた素材は**切り詰めず** `413 ASSET_TOO_LARGE` を返し、`bytes` を添える。
  ③は「取得できなかった」として保留する（途中まで取れたものを素材として使わない）。
- 動画は Range 必須ではないが、③が使うなら①は対応する。

## 7. 失敗と再試行

| 状態 | 意味 | ③の動き |
| --- | --- | --- |
| 401 | 鍵が違う・失効 | **再試行しない。**止めて人へ上げる |
| 403 | tenant/store 範囲外、権利未確認、停止中 | 再試行しない |
| 404 | 素材が無い | 再試行しない |
| 409 | 版が違う | 再試行しない。正しい版で取り直す |
| 410 | 権利の期限切れ | 再試行しない。**保持している複製を削除** |
| 413 | 上限超過 | 再試行しない |
| 429 / 5xx | 一時的 | **最大2回**まで再試行（1秒 → 4秒） |

- HTTP timeout は **10秒**。
- ③は 4xx を「たぶん一時的」として再試行しない。
- ①は失敗応答に素材の中身・保存先・鍵を含めない。

## 8. 失効・取消のとき

- ①が素材を停止したら、以後この口は 403 / 410 を返す。
- ③は**そのとき保持している複製を削除**し、その素材から作った制作物に
  「元素材が失効」の印を付けて、**人の判断待ちにする**（自動で作り直さない・自動で消さない）。
- ⚠️ 既に承認済み・書き出し済みの制作物をどう扱うかは**未決定**。
  ①の `approve` は承認後に素材が変われば承認を無効化する設計だが、
  ③側で既に外部へ出たものの扱いは、実運用の前に若林さんの判断が要る。

## 9. まだ決まっていないこと

| 項目 | 状態 |
| --- | --- |
| `{DASHBOARD_ORIGIN}` の実値 | 未定。配備先が決まってから |
| `CREATIVE_ASSET_READ_KEY` の発行・登録 | **未実施。若林さんの承認待ち** |
| 実素材の保存先 | 現在は暗号化して Vercel Blob。実運用でこのままかは未確定 |
| `rights.expires_at` の正本 | ①の台帳に項目が無い。素材登録時に取る運用と画面が要る |
| `store_id` の正式な体系 | ①は SalonOne の店舗IDを使っているが、③の `store_ids` との対応は未確認 |
| ③からの到達経路 | ③が配備されていないため、どのIPから来るのか未確定。 IP制限の可否も未確定 |
| 監査 | ①はこの口のアクセスを監査に残す予定。保存先・保持期間は未決定 |

## 10. ①が実装するときの前提

- 新しいAPIファイルは作らない（Vercelの関数上限）。`api/plan-store.js` のディスパッチに足すか、
  固定 path を要求されるなら1本だけ新設して既存を統合する。**着手前に相談します。**
- 実装しても既定は**閉**。`CREATIVE_ASSET_READ_KEY` が未設定のあいだは 503 を返し、
  「鍵が無いから開いていない」と分かる状態にする。
- 実素材を扱う前に、①の画面で「権利の確認」と「利用期限」を人が入れられるようにする。
  期限の項目が無いまま `expires_at` を返すことはしない（分からないものを埋めない）。
