# 日報・集客速報の自動送信（座組み）

SalonOne の数字を、スタッフの手打ちではなく、決まったテンプレートでグループチャットへ出すしくみ。
この文書は**設計と現在地**を書いたもの。実際に流れるかどうかはフラグと店舗ごとの設定で決まる。

## 1. いまの状態

| | 状態 |
| --- | --- |
| 文面の組み立て | 実装済み・検証済み（合成データ 69件） |
| 設定・送信のAPI | 実装済み・検証済み（サーバー側 31件） |
| 設定画面 | 実装済み・画面検証済み（39件） |
| **実際の送信** | **止まっている**（フラグ `cc_daily_report` が既定OFF） |
| 「集計実行」の受け取り | **未確認**（下記 4 章） |

コード保存とデモでの確認は済んでいる。**本番で実際に送ったことはまだ無い。**

## 2. 3つの期間（期間ごとに別のテンプレート）

店舗は `prep → preopen → open` の順に進む。どの期間かは**店舗ごとのプレオープン日・本オープン日と今日の日付**で
自動的に決まる（`phaseOf`）。日付を入れていない店舗＝既存店なので、常に `open`。

| 期間 | いつ | 出るもの |
| --- | --- | --- |
| `prep` オープン前 | プレオープン日より前 | 集客の積み上げ（売上は出さない） |
| `preopen` プレオープン期間 | プレオープン日〜本オープン前日 | セラピスト別・店舗合計の比較テーブル |
| `open` オープン後 | 本オープン日以降 | 通常の日報 |

### ① オープン前 集客レポート（`prep`）

```
【オープン前 集客レポート】テスト院
2026/09/19（土） 19:00時点・日本時間

プレオープン 9/27(日)　／　本オープン 10/1(木)

■ 目標まで、あと54名
　目標150名・集客目標の締切 9/30(水)
　累計の有効予約人数 96名 ／ 目標 150名　達成率 64%
　（10月の新規来店予約が対象・取消・重複を除く）

■ 本日の動き
　新規予約 9件 ／ キャンセル 2件 ／ 増減 +7件

■ 媒体別の予約状況（本日 00:00〜19:00）
　Meta広告　新規 5件 ／ 取消 1件 ／ 累計 52名
　チラシ　新規 2件 ／ 取消 1件 ／ 累計 24名
　Google検索　新規 1件 ／ 取消 0件 ／ 累計 12名
　合計　新規 9件 ／ 取消 2件 ／ 累計 96名

■ 累計広告費・獲得単価（2026-10-01〜2026-09-19・税別）
　Meta広告　¥104,000
　チラシ　¥48,000
　合計広告費 ¥152,000　／　CPA（有効予約ベース） ¥1,583 / 名

────────
出典: SalonOne marketing/by-channel
対象期間: 2026-10-01〜2026-10-31（10月来店予定ぶんの累計）
取得時刻: 2026-09-19 19:00 JST
※ 予約時点の数字です。来店・入会の結果はオープン後の日報で出します。
```

**「累計の有効予約人数」の定義**: `marketing/by-channel` を**対象月（本オープン月）の範囲**で取り、
`remaining_count`（＝予約 − 来店 − キャンセル＝まだ来ていない新規予約。SalonOne の「残新規」と一致）を合算したもの。

**「本日の新規／取消」の出し方**: SalonOne に「今日入った予約」を返す口は確認できていないので、
**毎日の累計スナップショットの差**で出す。初日は前日の記録が無いため増減だけ出さない（累計は出る）。

**広告費**: 既存の広告費欄（`?type=adspend` の店舗別・対象月）から読む。入っていない媒体は 0 円にせず、CPA も作らない。

### ② プレオープン日報（`preopen`）

見本（03 / 比較テーブル型）どおり、**セラピスト別・店舗合計の横並び**が主役。

```
【プレオープン日報】テスト院　プレオープン初日
対象日：2026/09/27（日）　集計実行 18:42・当日時点
プレオープン 9/27(日)　／　本オープン 10/1(木)

■ 本日売上 ¥89,750
　新規来店13名・入会4名・次回予約11名

■ セラピスト別・店舗合計
　　　　スタッフA ／ スタッフB　＝ 店舗合計
　本日売上（税抜）　¥32,000 ／ ¥57,750　＝ ¥89,750
　当日予約数（取消を含む）　7件 ／ 8件　＝ 15件
　新規来店数　5名 ／ 8名　＝ 13名
　キャンセル数　2件 ／ 0件　＝ 2件
　入会人数　1名 ／ 3名　＝ 4名
　入会率　20% ／ 37.5%　＝ 30.8%
　リピート数（次回予約）　5名 ／ 6名　＝ 11名
　リピート率　100% ／ 75%　＝ 84.6%
　前金あり　4名 ／ 5名　＝ 9名
　Google口コミ　2名 ／ 3名　＝ 5名

※ 入会率＝入会人数 ÷ 新規来店数
※ リピート率＝次回予約獲得人数 ÷ 新規来店数（再来店数とは別）
※ 売上：SalonOneの当日計上額・税抜　／　前金あり・口コミは当日獲得人数
```

**率は合計から計算し直す**（人ごとの率を平均しない）。30.8% は 4÷13、84.6% は 11÷13。
**1人でも欠けていれば合計を出さない**（欠けたまま足さない）。

**きっかけ**: 「集計実行」の完了後。オープン後の日報と同じ。
オープン前（`prep`）だけは「集計実行」が無いので指定時刻に出す。

#### 🔴 10項目のうち2つは取得元が未確認

| 項目 | 取得元 | 状態 |
| --- | --- | --- |
| 本日売上（税抜） | `sales/summary` の `by_staff.digest_sales` | ✅ |
| 当日予約数（取消を含む） | `marketing/by-staff.new_booking_count` | ✅ |
| 新規来店数 | `marketing/by-staff.new_visit_count` | ✅ |
| キャンセル数 | `marketing/by-staff.cancel_count` | ✅ |
| 入会人数 | `marketing/by-staff.purchase_count` | ✅（画面の「購入数」と同じ定義） |
| 入会率 | 計算（入会人数 ÷ 新規来店数） | ✅ |
| **リピート数（次回予約）** | — | **🔴 該当する項目が確認できていない** |
| リピート率 | 計算（上が取れないと出せない） | 🔴 |
| **前金あり** | — | **🔴 該当する項目が確認できていない** |
| Google口コミ | `sales/summary` の `by_staff.google_review_count` | ✅ |

未確認の2つは **0 で埋めず「未取得」** と書き、`missing` にも積む。
`lib/daily-report.js` の `NEXT_BOOKING_KEYS` / `PREPAID_KEYS` に候補キーを置いてあるが、
これは**推測**であり当たる保証はない。実際の項目名が分かったら、その配列に足すだけでよい。

⚠️ 売上が税抜か税込かも未確認。見本の脚注どおりの表記にしてある。

### ③ オープン後の日報（`open`）

SalonOne で「集計実行」を押すと売上に反映される。それを合図に、その日の結果を出す。

```
【日報】テスト院　9/19(土)

■ 売上
　売上合計　¥482,000
　新規 ¥180,000 ／ 既存 ¥302,000

■ 来店
　来店 24名（新規 6名 ／ 既存 18名）
　キャンセル 2件 ／ 無断 0件

■ 新規
　入会 3名（入会率 50%）
　目標 8名　あと 2名

■ 媒体別（新規予約）
　ホットペッパー　予約 4件 ／ 来店 3名 ／ 入会 2名
　Google　予約 2件 ／ 来店 2名 ／ 入会 1名

────────
出典: SalonOne sales/summary / marketing/by-channel
対象期間: 2026-09-19（1日）
取得時刻: 2026-09-19 21:05 JST
※ 「集計実行」の前に取得した場合は確定前の値になることがあります。
```

テンプレートは大枠。項目の増減・言い回しは `lib/daily-report.js` の
`buildPrepReport` / `buildPreOpenReport` / `buildOpenReport` だけを直せば変わる（他は触らなくてよい）。

## 3. どこから数字を取るか

| 項目 | 取得元 | 上流の項目名 |
| --- | --- | --- |
| 売上・新規/既存売上・来店数・キャンセル | `sales/summary` | `digest_sales`, `new_customer_sales`, `repeat_customer_sales`, `new_visit_count`, `repeat_visit_count`, `cancel_count`, `no_show_count` |
| 媒体別の予約・来店・入会 | `marketing/by-channel` | `booking_count`, `visit_count`, `join_count`, `cancel_count` |

売上は**入金ベース（digest）**を採用。画面の売上表示と同じ定義に揃えてある。

🔴 **取れなかった項目は 0 で埋めず「未取得」と書く。** 「0円の日」と「取れなかった日」を
同じ文面にしないため。主要な数字が1つも取れない日は**送らない**（中身の無い定型文を流さない）。
目標人数は設定値なので「取れた数字」には数えない。

## 4. 🔴 未確認：「集計実行」をどう受け取るか

**SalonOne に「集計実行が押された」を知らせる Webhook があるかは未確認。**
現在の API 一覧（`lib/salonone.js` の `ENDPOINTS`）にその通知口は無く、
押した操作そのものを外から見る手段は確認できていない。

そこで `trigger` を3つ用意し、いまは代理シグナルを既定にしている。

| trigger | 動き | 位置づけ |
| --- | --- | --- |
| `aggregate`（既定） | 当日の入金ベース売上が動き、その後30分動かなければ「集計が終わった」とみなす | **仮案**。推測なので文面にも確定前の可能性を書く |
| `time` | 指定時刻に送る | 確実だが、集計前だと数字が足りない |
| `manual` | 人が「いま送る」を押したときだけ | いちばん安全 |

Webhook が使えると分かれば、`detectAggregationRun()` を差し替えるだけでよい。文面側は変わらない。

**Cron の頻度（確認済み・2026-09-19）**: Vercel の契約は **Pro**。分単位で回せることを
オーナーの画面で確認した。`vercel.json` は **15分おき**（`*/15 * * * *`）にしてある。
これで `aggregate` は「集計実行のあと、売上が15〜45分動かなければ送る」という動きになる。

⚠️ SalonOne は 60回/分 の制限があるため、1回の自動実行で「集計実行」を見に行く店舗数に
上限（`DR_PROBE_CAP` = 20店舗）を設けている。あふれた店舗は次の実行に回る。

## 5. 勝手に流れないようにしている所

1. **フラグ `cc_daily_report` が既定OFF。** OFFの間は手動で押しても自動実行でも1通も出ない（下書きだけ返る）。
2. **店舗ごとの `enabled` も既定OFF。** チェックを入れた店舗だけが対象（全店一括にはできない）。
3. **送信先ルームが未設定なら送らない。** 個人あて（DM）は送信先の候補に出さない。
4. **同じ店舗・同じ日・同じ種類は1回だけ。** 自動実行が二重に走っても増えない。内容が同じなら「いま送る」を連打しても出ない。
5. **設定変更と手動送信は本部/rootだけ。** 店舗オーナーもエージェント用トークンも変えられない。
6. **自動投稿だと分かる差出人**（`日報（自動）`）で出す。人の発言と混ざらない。
7. 自動実行の結果に本文を含めない（ログに売上が残らない）。

## 6. 置き場所

| ファイル | 役割 |
| --- | --- |
| `lib/daily-report.js` | 文面・設定・二重送信の判定（純粋関数・I/Oなし） |
| `api/plan-store.js` の `?type=dailyreport` | 設定の保存、下書き、送信、自動実行 |
| `index.html` の `DailyReportPanel` | オーナー設定の中の設定画面 |
| `tests/daily-report.test.js` | 文面・期間判定・差分・広告費・セラピスト別（107件） |
| `tests/daily-report-api.test.js` | 権限・送信抑止・二重送信・期間別の取得範囲（38件） |
| `scripts/daily-report-screen-check.mjs` | 画面（57件・スタブAPI） |

## 7. 本番で動かすまでに要ること

1. `CRON_SECRET` が Vercel に入っていること（自動実行の合言葉）。
2. 店舗ごとにチェック＋送信先ルーム＋**プレオープン日・本オープン日・集客の目標と締切**を入れる。
   （日付を入れない店舗は既存店として、常に「オープン後の日報」になる）
3. 「いまの期間の下書き」で文面と数字を確かめる。
4. 数字と文面に納得できたら、オーナー設定でフラグ `日報の自動送信` をONにする。
   **ここを押すまで1通も流れない。**
5. 最初の1〜2日は1店舗だけONにして、実際に届いた内容を確かめてから広げる。
