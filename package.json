{
  "name": "naoru-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NAORU 総合ダッシュボード with Square API integration",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "node scripts/precompile.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "square": "39.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "vitest": "^3.0.0"
  }
}
