# CHAT_ROUTING_IMPLEMENTATION_STATUS — 設計と実装の対応表

**目的**: `CHAT_*.md`（設計）と、実際に動くコード（`lib/chat-*.js`）の**差分を隠さず**並べる。
**背景**: 設計6点と実装が**別々の作業で同時に進んだ**ため、名前と粒度が一部食い違っている。
どちらに寄せるかは人の判断が要るので、ここに列挙する。**勝手に片方を消していない。**

| | 作成 | 内容 |
|---|---|---|
| 設計6点 | 別セッション（commit `6484564`） | Phase分割・Intentスキーマ・20項目の監査 |
| 実装3点＋テスト86件 | このセッション（commit `4f4876b`） | 動く純粋関数とテスト |

---

## 1. 今あるもの（動作する）

| ファイル | 中身 | テスト |
|---|---|---|
| `lib/chat-recipients.js` | 宛先解決（正規化・候補・集合演算・権限フィルタ・安全判定） | 40件 |
| `lib/chat-lifecycle.js` | 店舗ルームの生成/改名/アーカイブ・在籍同期・イベントルーム | 28件 |
| `lib/chat-audit.js` | 送信/非送信の監査記録・絞り込み | 18件 |

`index.html` は**未変更**。画面は未着手（設計どおりR1以降）。

---

## 2. 設計 → 実装の対応

| 設計（`CHAT_*.md`） | 実装 | 状態 |
|---|---|---|
| `lib/chat-authz.js` `filterRecipients()` | `lib/chat-recipients.js` 内で `ctx.can` を注入して実施 | ⚠️ **名前が違う** |
| `lib/chat-resolver.js`（自然文→Intent） | 未実装（LLM側。`api/chat.js` の構造化出力） | ⏳ 未着手 |
| `lib/chat-recipients.js` 集合演算 | 実装済み（`resolveRecipients`） | ✅ |
| `lib/chat-recipients.js` `safetyCheck()` | `resolveRecipients` の戻り値 `status` に統合 | ⚠️ **関数として分かれていない** |
| `lib/chat-rooms.js` ライフサイクル | `lib/chat-lifecycle.js` として実装 | ⚠️ **ファイル名が違う** |
| `lib/chat-identity.js` 検証済みactor | `lib/actor.js`（`feature/cc-foundation`）が同じ役割 | ⚠️ **重複の恐れ** |
| 監査20項目 | 18項目 + `blocked` 系5項目 | ⚠️ **項目が一致していない** |

---

## 3. 食い違いの一覧（人の判断が必要）

### 3-1. 出力キーの名前

| 設計 | 実装 |
|---|---|
| `storeIds` | `shops: [{id, name}]` |
| `roomIds` | 未実装（店舗→ルームIDの変換はこれから） |
| `excludedStoreIds` | `excludedShops: [{id, name}]` |
| `deniedStoreIds` | `outOfScope: [{shop, code}]` |
| `ambiguous` | `ambiguities: [{query, candidates}]` + `unresolved: [{query}]` |
| `confidence` | **なし**（LLM側の値なので解決結果には持たせていない） |

→ **提案**: 設計側の名前（`storeIds` 等）に寄せる。ID配列のほうが監査・再送で扱いやすい。
表示名が必要な画面側は `dir` から引ける。**変更は小さく、テストで守られている。**

### 3-2. 曖昧と不明を分けているか

- 設計: `ambiguous` の1つにまとめる
- 実装: `ambiguities`（候補が複数）と `unresolved`（候補が0）に**分けている**

→ **提案**: 実装を残す。画面の文言が変わるため（「2人います→選んでください」と
「該当がありません→言い直してください」は、人がとる行動が違う）。

### 3-3. 大量送信のしきい値

| | 設計 | 実装 |
|---|---|---|
| 二重確認 | 50ルーム超 または 300名超 | **20名以上** または全社一斉 |
| owner/manager の上限 | 20ルーム | owner **200件**（`maxRecipients`）／manager は一斉送信不可 |

→ **要判断**。実装のほうが厳しい。運用の実感で決めるべき数字なので、
朝に「何名から確認を出すか」を決めてほしい。**設定値にするのが妥当**（テナント毎に上書き可）。

### 3-4. 権限の判断源

- 設計: `lib/chat-authz.js` を唯一の判断源とする
- 実装: `lib/authz.js`（`feature/cc-foundation`）の `can()` を**注入**する。
  注入が無い場合のみ、モジュール内の保守的な `fallbackCan` が働く

→ **提案**: 実装を残す。チャット専用の権限モジュールを新設すると、
`AUTHORIZATION_PLAN.md` の権限表と**二重管理**になり、いずれ食い違う。
チャットの操作（`chat.send` 等10種）は既に `lib/authz.js` に入れてある。

### 3-5. 店舗ルームの主キー

- 設計: `store_id` を主キーにする（改名でルームが分裂しない）
- 実装: 既存の `storeRoomId(店舗名)` を**そのまま使い**、改名は `rename`（名前だけ変更）で吸収

→ **要判断**。設計のほうが正しいが、**既存データの移行が要る**
（現行の `store_<店舗名>` を `store_<store_id>` に付け替える）。
移行は別PRにすべき。今の実装は「改名でルームが分裂しない」という目的だけは満たしている。

---

## 4. 実装で分かったこと（設計に無かった論点）

### 4-1. 🔴 店舗一覧が空で返ったら、全店ルームを畳んでしまう

SalonOne API が失敗して `[]` を返すと、素直な実装は「全店舗が消えた」と解釈して
**全店舗ルームをアーカイブする**。連絡経路の全損になる。
→ `planStoreRooms` は `shopList.length === 0` のとき archive を一切出さない（テストで固定）。

### 4-2. 🔴 権限判定を2段に分けないと、説明できない結果になる

宛先ごとの判定に `chat.broadcast` を使うと、スタッフが2店舗を指定したとき
**自店も含めて全部が消える**（スタッフは broadcast の役割を持たないため）。
→ 宛先ごとは常に `chat.send`、一斉送信の可否は別に判定する。

### 4-3. 除外語の曖昧さは、宛先の曖昧さより危険

「恵比寿は除いて」で2件該当したまま送ると、**送ってはいけない先に届く**。
→ 除外語が曖昧・不明でも全体を止める。

### 4-4. 一部だけ送る、をしてはいけない

「田中さんと佐藤さんに」で田中さんが2人いたとき、佐藤さんにだけ先に送ると、
依頼者も受け手も「届いていない」ことに気づけない。
→ 1件でも曖昧なら全体を止める。

---

## 5. 次にやること（提案）

| 順 | 内容 | 影響 |
|---|---|---|
| 1 | §3 の食い違いを朝に決める（特に 3-3 の数字と 3-5 の主キー） | 判断のみ |
| 2 | 出力キーを設計側の名前へ寄せる | ロジックのみ・テストあり |
| 3 | `api/chat.js` に Intent 構造化出力を足す（`lib/chat-resolver.js`） | 新規・フラグ下 |
| 4 | 送信前プレビューUI（`index.html`） | **要注意**: 巨大ファイル。Meta開発との競合 |
| 5 | 店舗ルーム主キーの移行（`store_id` 化） | **要注意**: 既存データの移行 |

**1〜3 は本番影響ゼロで進められる。4〜5 は承認が要る。**

---

## 6. ⚠️ PR #383 との重複（朝いちで判断が必要）

同じ夜に、別セッションが **PR #383「Phase 1: チャット権限（5ロール開放＋サーバー側認可の土台）」**
を作成している（`feature/chat-ai-routing-phase1`・**draft ではない**）。
内容が `feature/cc-foundation`（PR #379）の認可と**重複している**。

| PR #383 | PR #379（cc-foundation） | 状態 |
|---|---|---|
| `lib/chat-authz.js`（402行・新規） | `lib/authz.js`（chat操作10種を追加済み） | 🔴 **権限表が二重になる** |
| `lib/chat-identity.js`（130行・新規） | `lib/actor.js`（同じ役割・60秒キャッシュ付き） | 🔴 **本人確認が二重になる** |
| `api/plan-store.js`（+63行） | `api/plan-store.js`（認可の組み込み済み） | ⚠️ 自動マージは成功 |
| `index.html`（+51行） | `index.html`（CCブロック追加済み） | ⚠️ **競合1箇所**（解消可能） |

**試しにマージした結果**: `index.html` に競合1件のみ。技術的には解消できる。
**問題は競合ではなく、権限表が2つできること。** いずれ必ず食い違い、
「画面では送れるのにAPIが弾く」「その逆」が起きる。

### 判断してほしいこと

| 案 | 内容 |
|---|---|
| **A（推奨）** | `lib/authz.js` を唯一の判断源にし、#383 の `chat-authz.js` / `chat-identity.js` は取り込まない。#383 の残り（5ロール開放のUI・`api/plan-store` の組み込み）だけを活かす |
| B | #383 を先に入れ、`cc-foundation` の認可を `chat-authz.js` に寄せる |
| C | 両方入れて後で統合する（**非推奨**。二重管理の期間が生まれる） |

### さらに注意

PR #383 のタイトルは「**5ロール開放**」で、内容にスタッフへのチャット開放が含まれる。
今夜の禁止事項「Staffへの本番Chat権限開放」に該当するため、**マージしていない**。
マージの可否は人が判断すること。
