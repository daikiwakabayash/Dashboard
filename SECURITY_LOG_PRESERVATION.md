# SECURITY_LOG_PRESERVATION — アクセスログ保全の依頼（管理者向け）

**作成**: ①（共通基盤担当） / **状態**: **①では確認できません。管理者の操作が必要です**

> ⚠️ **ログが見られない／残っていないことを「不審アクセスなし」と判断しないでください。**
> 本書は「まだ分かっていない」ことを明示し、確認をお願いするためのものです。

---

## 0. ①が確認できたこと／できないこと

| | 状態 |
|---|---|
| 未認証で取得できた**経路**の特定 | ✅ できた（実際にHTTPで確認・下表） |
| 経路を塞いだこと | ✅ できた（本番で403を確認） |
| **誰が・いつ・何回アクセスしたか** | ❌ **できない**（①にVercelのログ参照権限がありません） |
| CDNキャッシュの全エッジ状況 | ❌ できない（1エッジで `x-vercel-cache: MISS` を確認したのみ） |

→ **漏えいの有無は判断できません。** 以下の保全をお願いします。

---

## 1. 対象 Project / Domain

| 種別 | 値 | 備考 |
|---|---|---|
| Vercel Project | `dashboard` | **本番。スタッフが使用中** |
| Production Domain | `dashboard-daikiwakabayashs-projects.vercel.app` | 実際に確認した本番URL |
| 同 Project | `dashboard-7x9k` | `dashboard-7x9k.vercel.app` は**環境変数が空**（データ無し） |
| 同 Project | `dashboard-sae7` | 同上 |
| **Preview / Deployment URL** | **`*.vercel.app` の各デプロイURL（全件）** | ⚠️ Preview は**本番と同じKVを共有**していたため、Preview URL 経由でも同じデータが取得可能でした |

> ⚠️ **Preview URL を軽視しないでください。** 本番と同じ保存先を参照していたため、
> 古いPreview URL からでも同じ社内データが取得できた可能性があります。

---

## 2. 確認すべき期間

| 経路 | いつから取得可能だったか | 塞いだ日時 |
|---|---|---|
| `?type=chat`（ルーム・本文・名簿・ノート） | チャット機能の導入時から | 2026-09-18（PR #390） |
| `?type=chat&img=` / `?type=profile&img=`（画像） | 同上 | 2026-09-18（PR #390） |
| `?type=profile`（スタッフ名簿） | 同上 | 2026-09-18（PR #390） |
| `?type=thanksgift` ほか13種 | 各機能の導入時から | 2026-09-18（PR #393） |

**取得できた期間**: ①は**0日ぶんも取得できていません**（参照権限なし）。
**Vercelのログ保持期間はプランに依存します**（Hobby は短期）。**まず保持期間の確認を最優先**にしてください。

---

## 3. 確認してほしいアクセス経路（優先順）

```
1) GET /api/plan-store?type=chat                 … ルーム一覧・本文・名簿・ノート
2) GET /api/plan-store?type=chat&img=…           … 画像（&raw=1 で生バイナリ）
3) GET /api/plan-store?type=profile&img=…        … 画像の**迂回経路**（同じ保存先を読む）
4) GET /api/plan-store?type=profile              … スタッフ名簿
5) GET /api/plan-store?type=thanksgift           … 投票者の氏名（**匿名性が前提**）
6) GET /api/plan-store?type=allowance            … 氏名・金額・生産性
7) GET /api/plan-store?type=adspend / knowledge / faq / events / board / presence
8) POST /api/plan-store （上記 type への書き込み）… 改ざんの有無
```

---

## 4. 正常な社内アクセスとの区別

社内の正規利用と、外部からの取得を見分ける手がかりです。

| 観点 | 正常な社内アクセス | 注意すべきアクセス |
|---|---|---|
| Referer | ダッシュボード本体（同一オリジン） | 無し／外部サイト |
| User-Agent | 一般的なブラウザ | `curl` `python-requests` `wget` `Go-http-client` 等 |
| アクセス様式 | 画面遷移に沿って複数typeを連続取得 | **単一typeだけを繰り返し取得**／全typeを機械的に順番に取得 |
| 頻度 | チャット6秒・掲示板8秒・プレゼンス30秒の**規則的なポーリング** | 不規則・短時間の大量取得 |
| 画像 | メッセージ表示に伴い**関連するIDだけ**取得 | **IDを列挙して連続取得** |
| 時間帯 | 営業時間帯に集中 | 深夜・早朝の機械的アクセス |
| IP | 店舗・自宅・モバイル回線 | データセンター・VPN・海外 |

**特に注目**: 「`?type=` を次々に変えて1回ずつ叩く」パターンは、正常利用ではほぼ発生しません。

---

## 5. 旧CDNキャッシュの確認と purge 対象案

チャット画像は **`Cache-Control: public, max-age=31536000, immutable`** で配信されていました（現在は `private, no-store` に修正済み）。

| 確認項目 | ①の結果 | お願いしたいこと |
|---|---|---|
| 現在の応答 | ✅ 既知の画像IDで **403**・`x-vercel-cache: MISS` | 他のエッジでも同様か |
| エッジキャッシュの残存 | ❓ 1エッジしか確認できていません | **Vercel のキャッシュ purge** |
| 利用者のブラウザキャッシュ | ⚠️ `immutable` のため**最大1年**残り得ます | ①では消せません（下記参照） |

### purge 対象案（**絞り込み済み・別承認が必要**）

```
/api/plan-store?type=chat&img=*
/api/plan-store?type=chat&img=*&raw=1
/api/plan-store?type=profile&img=*
```

- **全体purgeは不要**です。上記の画像配信パスのみで足ります
- 実行すると一時的に画像の再取得が発生しますが、**機能は壊れません**（認証付きで再取得されます）
- ブラウザ側のキャッシュは purge できません。確実に無効化するには**画像IDの振り直し**が必要ですが、
  既存メッセージの表示が壊れるため**推奨しません**（影響と利益が釣り合いません）

---

## 6. 管理者にお願いしたい操作（この順で）

1. **ログの保持期間を確認**（Vercel → Project → Logs。プランにより異なります）
2. 期間内であれば、§3 の経路への**アクセス記録をエクスポートして保全**
   - 対象: `dashboard` / `dashboard-7x9k` / `dashboard-sae7` の **全Deployment URL**
3. §4 の観点で**正常利用と区別**し、該当があれば①へ**件数と特徴のみ**共有してください
   - ⚠️ **ログ本文・個人情報・メッセージ本文をGitHubへ貼らないでください**
4. §5 の purge 対象について**実行可否をご判断**ください（別承認）

## 7. ①が行っていないこと（明示）

- Vercelのログ参照（**権限がありません**）
- CDNキャッシュのpurge（**別承認が必要**）
- 本番データの削除・変更
- 「不審アクセスは無かった」という判断（**根拠がないため行いません**）
