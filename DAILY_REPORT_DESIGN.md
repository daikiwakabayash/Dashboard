# 日報・集客速報の自動送信（座組み）

SalonOne の数字を、スタッフの手打ちではなく、決まったテンプレートでグループチャットへ出すしくみ。
この文書は**設計と現在地**を書いたもの。実際に流れるかどうかはフラグと店舗ごとの設定で決まる。

## 1. いまの状態

| | 状態 |
| --- | --- |
| 文面の組み立て | 実装済み・検証済み（合成データ 69件） |
| 設定・送信のAPI | 実装済み・検証済み（サーバー側 31件） |
| 設定画面 | 実装済み・画面検証済み（39件） |
| **実際の送信** | **止まっている**（フラグ `cc_daily_report` が既定OFF） |
| 「集計実行」の受け取り | **未確認**（下記 4 章） |

コード保存とデモでの確認は済んでいる。**本番で実際に送ったことはまだ無い。**

## 2. 2種類の送信

### ① 営業後の日報（`closing`）

SalonOne で「集計実行」を押すと売上に反映される。それを合図に、その日の結果を出す。

```
【日報】テスト院　9/19(土)

■ 売上
　売上合計　¥482,000
　新規 ¥180,000 ／ 既存 ¥302,000

■ 来店
　来店 24名（新規 6名 ／ 既存 18名）
　キャンセル 2件 ／ 無断 0件

■ 新規
　入会 3名（入会率 50%）
　目標 8名　あと 2名

■ 媒体別（新規予約）
　ホットペッパー　予約 4件 ／ 来店 3名 ／ 入会 2名
　Google　予約 2件 ／ 来店 2名 ／ 入会 1名

────────
出典: SalonOne sales/summary / marketing/by-channel
対象期間: 2026-09-19（1日）
取得時刻: 2026-09-19 21:05 JST
※ 「集計実行」の前に取得した場合は確定前の値になることがあります。
```

### ② オープン前の集客速報（`preopen`）

「集計実行」が無い時間帯。予約表から人数と流入媒体を見て、目標まであと何人かを返す。
セラピスト・オーナー・本部が同じ数字を見られるように、同じルームへ出す。

```
【本日の集客速報】テスト院　9/19(土) 09:30 時点

　本日の新規予約　5名
　目標 8名　→　あと 3名

■ 媒体別（本日の新規予約）
　ホットペッパー　3件
　Google　1件
　LINE　1件

────────
出典: SalonOne marketing/by-channel
対象期間: 2026-09-19（1日）
取得時刻: 2026-09-19 09:30 JST
※ 予約時点の数字です。来店・入会の結果は営業後の日報で出します。
```

テンプレートは大枠。項目の増減・言い回しは `lib/daily-report.js` の
`buildClosingReport` / `buildPreOpenReport` だけを直せば変わる（他は触らなくてよい）。

## 3. どこから数字を取るか

| 項目 | 取得元 | 上流の項目名 |
| --- | --- | --- |
| 売上・新規/既存売上・来店数・キャンセル | `sales/summary` | `digest_sales`, `new_customer_sales`, `repeat_customer_sales`, `new_visit_count`, `repeat_visit_count`, `cancel_count`, `no_show_count` |
| 媒体別の予約・来店・入会 | `marketing/by-channel` | `booking_count`, `visit_count`, `join_count`, `cancel_count` |

売上は**入金ベース（digest）**を採用。画面の売上表示と同じ定義に揃えてある。

🔴 **取れなかった項目は 0 で埋めず「未取得」と書く。** 「0円の日」と「取れなかった日」を
同じ文面にしないため。主要な数字が1つも取れない日は**送らない**（中身の無い定型文を流さない）。
目標人数は設定値なので「取れた数字」には数えない。

## 4. 🔴 未確認：「集計実行」をどう受け取るか

**SalonOne に「集計実行が押された」を知らせる Webhook があるかは未確認。**
現在の API 一覧（`lib/salonone.js` の `ENDPOINTS`）にその通知口は無く、
押した操作そのものを外から見る手段は確認できていない。

そこで `trigger` を3つ用意し、いまは代理シグナルを既定にしている。

| trigger | 動き | 位置づけ |
| --- | --- | --- |
| `aggregate`（既定） | 当日の入金ベース売上が動き、その後30分動かなければ「集計が終わった」とみなす | **仮案**。推測なので文面にも確定前の可能性を書く |
| `time` | 指定時刻に送る | 確実だが、集計前だと数字が足りない |
| `manual` | 人が「いま送る」を押したときだけ | いちばん安全 |

Webhook が使えると分かれば、`detectAggregationRun()` を差し替えるだけでよい。文面側は変わらない。

**もうひとつの未確認**: Vercel の Cron を何分おきに回せるかは契約プラン次第。
いまは `vercel.json` に1日1回ずつ（速報 10:00 JST / 日報 22:00 JST）を入れてある。
`aggregate` を本当に「押した直後」に近づけるには、数分おきに回せるプランが要る。
1日1回のままだと `aggregate` は実質 `time` と変わらない。

## 5. 勝手に流れないようにしている所

1. **フラグ `cc_daily_report` が既定OFF。** OFFの間は手動で押しても自動実行でも1通も出ない（下書きだけ返る）。
2. **店舗ごとの `enabled` も既定OFF。** チェックを入れた店舗だけが対象（全店一括にはできない）。
3. **送信先ルームが未設定なら送らない。** 個人あて（DM）は送信先の候補に出さない。
4. **同じ店舗・同じ日・同じ種類は1回だけ。** 自動実行が二重に走っても増えない。内容が同じなら「いま送る」を連打しても出ない。
5. **設定変更と手動送信は本部/rootだけ。** 店舗オーナーもエージェント用トークンも変えられない。
6. **自動投稿だと分かる差出人**（`日報（自動）`）で出す。人の発言と混ざらない。
7. 自動実行の結果に本文を含めない（ログに売上が残らない）。

## 6. 置き場所

| ファイル | 役割 |
| --- | --- |
| `lib/daily-report.js` | 文面・設定・二重送信の判定（純粋関数・I/Oなし） |
| `api/plan-store.js` の `?type=dailyreport` | 設定の保存、下書き、送信、自動実行 |
| `index.html` の `DailyReportPanel` | オーナー設定の中の設定画面 |
| `tests/daily-report.test.js` | 文面と判定（69件） |
| `tests/daily-report-api.test.js` | 権限・送信抑止・二重送信（31件） |
| `scripts/daily-report-screen-check.mjs` | 画面（39件・スタブAPI） |

## 7. 本番で動かすまでに要ること

1. `CRON_SECRET` が Vercel に入っていること（自動実行の合言葉）。
2. 店舗ごとに「日報を自動送信する」にチェック＋送信先ルーム＋1日の新規目標を入れる。
3. 「日報の下書き」「速報の下書き」で文面と数字を確かめる。
4. 数字と文面に納得できたら、オーナー設定でフラグ `日報の自動送信` をONにする。
   **ここを押すまで1通も流れない。**
5. 最初の1〜2日は1店舗だけONにして、実際に届いた内容を確かめてから広げる。
