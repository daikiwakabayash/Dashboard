# 整体院経営管理ダッシュボード - セットアップ手順書

このドキュメントでは、スプレッドシートとGoogle Apps Script (GAS) を使用してダッシュボードにデータを連携する手順を説明します。

## 📋 目次

1. [スプレッドシートの準備](#1-スプレッドシートの準備)
2. [Google Apps Scriptのセットアップ](#2-google-apps-scriptのセットアップ)
3. [フロントエンドの設定](#3-フロントエンドの設定)
4. [動作確認](#4-動作確認)
5. [トラブルシューティング](#5-トラブルシューティング)

---

## 1. スプレッドシートの準備

### 1.1 スプレッドシートの作成

1. [Google スプレッドシート](https://sheets.google.com) を開く
2. 「空白」から新しいスプレッドシートを作成
3. スプレッドシートの名前を「整体院経営管理データ」などに変更

### 1.2 ヘッダー行の設定

**1行目に以下のヘッダーを正確に入力してください：**

```
日付	名前	院名	新規数	新規入会数	入会率	新規平均単価	新規合計売上	先月会員数	今月退会数	退会率	GAP	G口コミ	H口コミ	個別税込売上	継続売上	健康診断	健康手当	フルマラソン	勉強代手当	誕生日手当	子供手当	歯クリーニング	家族手当	イベントアクセス手当	ウェルカムファミリー手当	目標達成手当	グーグル口コミ	HPB口コミ	個人生産性（税込）	成約率	(広告費率)	広告費率	紹介数	タスク漏れ	T	各セラピスト合計得点	達成/未達成	達成回数	健康手当st	健康残高	勉強手当st	勉強残高	seisannsei	saitei	total
```

> **重要**: ヘッダー名は正確に入力してください。スペルミスや全角・半角の違いがあるとデータが正しく読み込まれません。

### 1.3 データの入力

2行目以降にデータを入力してください。

#### データ入力の例：

| 日付 | 名前 | 院名 | 新規数 | 新規入会数 | 入会率 | ... |
|------|------|------|--------|------------|--------|-----|
| 2026年1月 | 山田太郎 | 大森 | 15 | 10 | 67 | ... |
| 2026年1月 | 佐藤花子 | 千葉駅 | 20 | 15 | 75 | ... |

#### データ型の説明：

- **日付**: `YYYY年M月` 形式（例: 2026年1月）
- **名前**: テキスト
- **院名**: テキスト
- **数値項目**: 半角数字（例: 15, 10, 67）
- **達成/未達成**: "達成" または "未達成"

### 1.4 スプレッドシートIDの取得

1. スプレッドシートのURLを確認
2. URLは以下の形式です：
   ```
   https://docs.google.com/spreadsheets/d/[スプレッドシートID]/edit
   ```
3. `[スプレッドシートID]` の部分をコピーして保存

**例：**
```
https://docs.google.com/spreadsheets/d/1a2b3c4d5e6f7g8h9i0j/edit
                                      ↑この部分がスプレッドシートID
```

---

## 2. Google Apps Scriptのセットアップ

### 2.1 Apps Scriptエディタを開く

1. スプレッドシートを開く
2. メニューから **「拡張機能」** → **「Apps Script」** を選択
3. 新しいタブでApps Scriptエディタが開きます

### 2.2 GASコードの貼り付け

1. エディタに表示されているデフォルトコードを削除
2. `gas-code.gs` ファイルの内容を全てコピー
3. Apps Scriptエディタに貼り付け

### 2.3 設定の変更

コードの先頭部分を編集します：

```javascript
// ここを編集
const SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID_HERE'; // ← 1.4で取得したIDを貼り付け
const SHEET_NAME = 'シート1'; // ← 実際のシート名に変更
```

**例：**
```javascript
const SPREADSHEET_ID = '1a2b3c4d5e6f7g8h9i0j';
const SHEET_NAME = 'シート1';
```

### 2.4 プロジェクト名の設定

1. 左上の「無題のプロジェクト」をクリック
2. 「整体院ダッシュボードAPI」などの名前に変更

### 2.5 テスト実行（オプション）

1. エディタ上部の関数選択で `testGetData` を選択
2. 「実行」ボタンをクリック
3. 初回は権限の承認が必要です：
   - 「権限を確認」をクリック
   - Googleアカウントを選択
   - 「詳細」をクリック → 「[プロジェクト名]（安全ではないページ）に移動」
   - 「許可」をクリック
4. 実行ログを確認（メニュー: 表示 → ログ）

### 2.6 デプロイ

1. 右上の **「デプロイ」** → **「新しいデプロイ」** をクリック
2. 左側の⚙️（歯車アイコン）をクリック → **「ウェブアプリ」** を選択
3. 以下の設定を行います：
   - **説明**: 「整体院ダッシュボードAPI v1」など
   - **次のユーザーとして実行**: **「自分」** を選択
   - **アクセスできるユーザー**: **「全員」** を選択
4. **「デプロイ」** をクリック
5. 初回は再度権限の承認が必要な場合があります
6. **「ウェブアプリのURL」** をコピーして保存

**ウェブアプリのURL例：**
```
https://script.google.com/macros/s/AKfycbxxx.../exec
```

> **重要**: このURLは後で使用するので必ず保存してください。

---

## 3. フロントエンドの設定

### 3.1 GAS API URLの設定

1. `indx.html` ファイルを開く
2. 以下の行を見つけます：
   ```javascript
   const GAS_API_URL = 'YOUR_GAS_WEB_APP_URL_HERE';
   ```
3. `'YOUR_GAS_WEB_APP_URL_HERE'` を2.6で取得したウェブアプリのURLに置き換えます：
   ```javascript
   const GAS_API_URL = 'https://script.google.com/macros/s/AKfycbxxx.../exec';
   ```

### 3.2 ファイルの保存

変更を保存してください。

---

## 4. 動作確認

### 4.1 ダッシュボードを開く

1. `indx.html` をブラウザで開く
2. ローディングアニメーションが表示されます
3. データが正常に読み込まれると、ダッシュボードが表示されます

### 4.2 確認ポイント

✅ スプレッドシートのデータが正しく表示されているか
✅ フィルター（月、店舗、検索）が機能しているか
✅ KPIメトリクスが正しく計算されているか
✅ テーブルにデータが表示されているか

### 4.3 データの更新

1. スプレッドシートでデータを変更
2. ダッシュボードの「再読み込み」ボタンをクリック
3. 変更が反映されることを確認

---

## 5. トラブルシューティング

### 問題: ローディングが終わらない

**原因:**
- GAS_API_URLが正しく設定されていない
- ネットワーク接続の問題

**解決方法:**
1. ブラウザの開発者ツール（F12）を開く
2. コンソールタブでエラーメッセージを確認
3. GAS_API_URLが正しいか確認
4. GASのウェブアプリURLをブラウザで直接開いてみる

### 問題: 「データ取得エラー」が表示される

**原因:**
- スプレッドシートIDが間違っている
- シート名が間違っている
- GASの権限が正しく設定されていない

**解決方法:**
1. `gas-code.gs` の `SPREADSHEET_ID` と `SHEET_NAME` を確認
2. Apps Scriptエディタで `checkSheetStructure` 関数を実行してシート構造を確認
3. GASを再デプロイしてみる

### 問題: データが表示されない（空）

**原因:**
- スプレッドシートにデータがない（ヘッダーのみ）
- ヘッダー名が正確に入力されていない

**解決方法:**
1. スプレッドシートの2行目以降にデータが入力されているか確認
2. ヘッダー名が正確に入力されているか確認（全角・半角、スペースに注意）
3. Apps Scriptエディタで `testGetData` 関数を実行してログを確認

### 問題: 一部のデータが正しく表示されない

**原因:**
- データ型が間違っている（数値に文字が入っている等）
- ヘッダー名のスペルミス

**解決方法:**
1. スプレッドシートの該当列のデータを確認
2. 数値項目には半角数字のみを入力
3. ヘッダー名を「1.2 ヘッダー行の設定」と照らし合わせて確認

### 問題: CORSエラー

**原因:**
- ブラウザのセキュリティポリシー

**解決方法:**
1. ローカルサーバーで実行する（例: `python -m http.server 8000`）
2. または、Webサーバーにアップロードして実行

---

## 6. Square API 75店舗連携セットアップ

### 6.1 全体の仕組み

```
┌──────────────────────────────────────────────────────┐
│  75店舗の構成例                                        │
│                                                      │
│  Squareアカウント1 → 恵比寿院, 渋谷院, ...  (10店舗)   │
│  Squareアカウント2 → 千葉駅院, 船橋院, ...  (10店舗)   │
│  Squareアカウント3 → 大森院, 品川院, ...    (10店舗)   │
│  ...                                                 │
│  SquareアカウントN → ○○院, △△院, ...       (残り店舗)  │
│                                                      │
│  → 全アカウントを SQUARE_TOKENS に登録               │
│  → フロントエンドがバッチ並列で高速取得              │
└──────────────────────────────────────────────────────┘
```

### 6.2 Square Developer アカウントの準備

**店舗ごとにSquareアカウントがある場合:**

1. 各Squareアカウントの **Developer Dashboard** にアクセス
   - https://developer.squareup.com/apps
2. アプリケーションを作成（または既存のアプリケーションを使用）
3. **Production** タブ → **Access Token** をコピー

**1つのSquareアカウントに複数店舗（ロケーション）がある場合:**
- 1つのアクセストークンで全ロケーションが自動取得されます
- `excludeLocations` で不要なロケーションを除外可能

### 6.3 SQUARE_TOKENS の設定

Vercelの環境変数に `SQUARE_TOKENS` を設定します。

**手順:**

1. **Vercelダッシュボード** → プロジェクト → **Settings** → **Environment Variables**
2. **Key**: `SQUARE_TOKENS`
3. **Value**: 以下のJSON配列形式

```json
[
  {
    "name": "恵比寿院",
    "token": "sq_live_XXXXXXXXXXXXXX",
    "env": "production"
  },
  {
    "name": "千葉駅院",
    "token": "sq_live_YYYYYYYYYYYYYY",
    "env": "production"
  },
  {
    "name": "大森院",
    "token": "sq_live_ZZZZZZZZZZZZZZ",
    "env": "production",
    "excludeLocations": ["株式会社SSiM", "テスト店舗"]
  }
]
```

**各フィールドの説明:**

| フィールド | 必須 | 説明 |
|-----------|------|------|
| `name` | はい | ダッシュボードに表示される店舗名 |
| `token` | はい | Square APIアクセストークン（`sq_live_`で始まる） |
| `env` | はい | `"production"` または `"sandbox"` |
| `excludeLocations` | いいえ | 除外するロケーション名のパターン配列 |

### 6.4 75店舗を効率的に登録するコツ

**1. アカウントのグループ化**

同じSquareアカウント配下の店舗は1エントリで管理されます。
例: 1つのアカウントに10店舗ある場合、SQUARE_TOKENSは1エントリだけで10店舗をカバーします。

```
75店舗 ÷ 10店舗/アカウント = 約8エントリで管理可能
```

**2. Vercel環境変数のサイズ制限に注意**

- Vercelの環境変数は **4KB** が上限
- 75店舗分のトークン(各アカウント約200文字) → 約8-15アカウント = 約1.6-3KB で十分収まります

**3. 段階的な追加**

一度に75店舗を追加する必要はありません:
1. まず5店舗で動作確認
2. 問題なければ追加していく
3. `/api/square/metrics?info=1` で登録済みアカウント一覧を確認

### 6.5 動作確認

**Step 1: アカウント一覧の確認**
```
https://your-app.vercel.app/api/square/metrics?info=1
```
登録された全アカウントが表示されます。

**Step 2: 個別アカウントのテスト**
```
https://your-app.vercel.app/api/square/metrics?account=0
```
最初のアカウントのデータを確認します。

**Step 3: バッチ取得のテスト**
```
https://your-app.vercel.app/api/square/metrics?accounts=0,1,2
```
複数アカウントを一括取得できることを確認します。

**Step 4: ダッシュボードで確認**

ダッシュボードのSquareページを開き:
- 全店舗が表示されるか
- プログレスバーが表示されるか
- データが段階的に表示されるか（プログレッシブ表示）

### 6.6 パフォーマンス最適化の仕組み

75店舗の読み込みを高速化するために、以下の4層の最適化が実装されています:

| 層 | 仕組み | 効果 |
|----|--------|------|
| **1. クライアントキャッシュ** | localStorage 30分TTL + stale-while-revalidate | 2回目以降は即座に表示 |
| **2. バッチ取得** | 3アカウントずつ1リクエストにまとめる | HTTPリクエスト数を1/3に削減 |
| **3. サーバーキャッシュ** | インメモリ 5分TTL | 同一アカウントの重複APIコール削減 |
| **4. ロケーション並列** | 店舗ごとのAPI呼び出しを3並列化 | 各アカウントの処理時間を約1/3に短縮 |
| **5. Vercel CDN** | Cache-Control: s-maxage=300 | CDNキャッシュで5分間再利用 |

**想定読み込み時間:**

| 状態 | 時間 |
|------|------|
| キャッシュヒット（30分以内） | **即座** (< 100ms) |
| stale cache + バックグラウンド更新 | **即座に表示** → 裏で10-20秒で更新 |
| Vercel CDNキャッシュ（5分以内） | **2-5秒** |
| サーバーキャッシュ（5分以内） | **3-8秒** |
| 初回 or キャッシュ切れ | **10-30秒**（バッチ並列取得） |

### 6.7 トラブルシューティング（75店舗固有）

**問題: 一部の店舗データが取得できない**
- `/api/square/metrics?info=1` でアカウント一覧を確認
- `/api/square/metrics?account=N` で個別にテスト
- Vercelのログ（Deployments → Functions タブ）でエラー詳細を確認

**問題: タイムアウトが発生する**
- Vercelの無料プランでは関数実行時間が10秒制限
- **Pro以上**のプランにアップグレード（60秒まで対応）
- `vercel.json` の `maxDuration: 60` で最大実行時間を設定済み

**問題: Squareのレート制限に引っかかる**
- バッチ内並列数は最大3に制限済み
- ロケーション並列も最大3に制限済み
- それでもエラーが出る場合は、アカウント数を減らしてバッチサイズを調整

---

## 補足情報

### データ更新の頻度

- スプレッドシートのデータを変更した後、ダッシュボードの「再読み込み」ボタンをクリックすると最新のデータが取得されます
- 自動更新を実装する場合は、`useEffect` に定期実行を追加してください

### セキュリティについて

- 現在の設定では「全員」がアクセス可能です
- より厳密な認証が必要な場合は、GASのアクセス設定を変更してください

### パフォーマンスの最適化

- データ量が多い場合、読み込みに時間がかかることがあります
- その場合は、GAS側でページネーションやキャッシュの実装を検討してください

---

## 🎉 完了

セットアップが完了しました！スプレッドシートでデータを管理し、ダッシュボードでビジュアル化して経営管理にご活用ください。

ご不明な点がございましたら、お気軽にお問い合わせください。
