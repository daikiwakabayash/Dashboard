# NAORU サブスクリプション管理 仕様書

## 概要

NAORUダッシュボードのサブスクリプション管理機能は、Square APIからサブスクリプションデータを取得し、各店舗の会員状況・収益・解約分析を可視化します。

---

## 用語定義

| 用語 | 定義 |
|------|------|
| **アクティブ会員** | 選択月末時点でサブスクリプションステータスが`ACTIVE`であり、過去にPAIDインボイスがある、または月額 > 0のサブスクを持つ顧客。顧客ID単位で重複排除。`PAUSED`（一時停止中）はアクティブに含まない。 |
| **新規入会** | 選択月にサブスクリプションを**初めて**開始した顧客。過去に別プランを持っていた顧客は含まない（→プラン変更に分類）。 |
| **プラン変更** | 過去にサブスクリプションを持っていた顧客が、選択月に新たなサブスクリプションを開始した場合。最初の入会月と異なる月に新しいサブスクが始まった場合が対象。 |
| **解約済み** | サブスクリプションステータスが`CANCELED`または`DEACTIVATED`で、解約日が選択月内の顧客。 |
| **一時停止** | サブスクリプションステータスが`PAUSED`で、停止日が選択月内の顧客。 |
| **月間解約率** | `(前月のアクティブ会員のうち、当月にアクティブでなくなった人数) ÷ 前月のアクティブ会員数 × 100`（%、小数点2桁） |
| **初月解約率** | サブスク決済を開始した月の**翌月**に決済がない（＝解約した）顧客の割合。例: 12月に決済開始 → 1月に決済なし = 初月解約。 |
| **3ヶ月解約率** | サブスク決済を開始した月から**3ヶ月後**に決済がない顧客の割合。例: 12月に決済開始 → 3月に決済なし = 3ヶ月解約。途中月は検証しない。 |
| **MRR** | Monthly Recurring Revenue。選択月のPAIDサブスクインボイス合計金額。 |
| **ARPU** | Average Revenue Per User。`MRR ÷ アクティブ会員数`。 |
| **LTV** | 顧客生涯価値。`ARPU ÷ (月間解約率 / 100)`。解約率が0の場合は`ARPU × 24`。 |
| **総売上高** | Square Payments APIから取得したCOMPLETED決済の合計金額 - 返金額。サブスク以外の決済（POS・端末・カードオンファイル等）も含む。 |
| **継続率 (N ヶ月)** | 入会後Nヶ月目に決済がある顧客の割合。 |

---

## KPI指標の計算方法

### アクティブ会員数
```
対象 = サブスクリプション.filter(
  isActiveAt(月末) AND (PAIDインボイスあり OR 月額 > 0)
)
会員数 = 顧客IDで重複排除したSet.size
```

### 新規入会数
```
対象 = サブスクリプション.filter(
  startDate が選択月 AND 決済実績あり AND プラン変更ではない
)
※プラン変更判定: 顧客の最初のサブスク開始月 ≠ 当該サブスクの開始月
```

### プラン変更数
```
対象 = サブスクリプション.filter(
  startDate が選択月 AND 決済実績あり AND プラン変更である
)
```

### 月間解約率
```
前月アクティブ = 前月末時点のアクティブ会員（顧客IDセット）
当月アクティブ = 当月末時点の全店舗横断アクティブ会員（顧客IDセット）
解約数 = 前月アクティブ - 当月アクティブ の差分人数
解約率 = 解約数 ÷ 前月アクティブ人数 × 100
```
- 店舗間移動は解約とカウントしない（全店舗横断でアクティブかどうかを判定）

### 初月解約率
```
eligible = 全顧客のうち、初回入会からN+1ヶ月が経過している人
churned = eligible のうち、初回入会の翌月に決済がない人
初月解約率 = churned ÷ eligible × 100
```
- プラン変更があっても、初回入会日からカウント
- 全店舗横断で決済有無を判定

### 3ヶ月解約率
```
eligible = 全顧客のうち、初回入会からN+3ヶ月が経過している人
churned = eligible のうち、初回入会の3ヶ月後に決済がない人
3ヶ月解約率 = churned ÷ eligible × 100
```
- 途中の月（1ヶ月目、2ヶ月目）は検証しない
- 3ヶ月後の決済有無のみで判定

### コホート分析
```
各入会月コホート:
  startCount = その月に初めて入会した顧客数
  retention[i] = i ヶ月後に決済がある顧客数 ÷ startCount × 100

※プラン変更があっても、最初の入会月のコホートに所属
※全店舗横断で決済有無を判定
```

### 継続率 (3/6/12ヶ月)
```
eligible = 初回入会からNヶ月が経過した全顧客
survived = eligible のうち、Nヶ月目に決済がある顧客
継続率 = survived ÷ eligible × 100
```

---

## 会員推移（月別トレンド）

12ヶ月分の月別推移を計算。各月は独立して直接計算（逆算ロジックは使用しない）。

| 項目 | 計算方法 |
|------|----------|
| 会員数 | 月末時点のアクティブ会員数（全期間PAIDまたは月額>0で絞込） |
| MRR | 当月のPAIDサブスクインボイス合計 |
| 新規入会 | 当月開始のサブスク（プラン変更除外、顧客ID重複排除） |
| プラン変更 | 当月開始で初回入会ではないサブスク（顧客ID重複排除） |
| 解約 | 前月アクティブ → 当月非アクティブの人数 |

### 棒グラフ（月別 入会・解約数）
- 緑バー: 新規入会数（プラン変更は含まない）
- 赤バー: 解約数
- プラン変更は棒グラフには含めない

---

## 顧客リスト タブ構成

| タブ | 表示内容 |
|------|----------|
| アクティブ | 選択月末時点でアクティブなサブスク会員 |
| 新規入会 | 選択月に初めてサブスクを開始した会員（プラン変更を除く） |
| **プラン変更** | 選択月にプラン変更で新たなサブスクを開始した会員 |
| 解約済み | 選択月内に解約（CANCELED/DEACTIVATED）された会員 |
| 一時停止 | 選択月内に一時停止（PAUSED）された会員 |
| 払戻し | 選択月にREFUNDED/PARTIALLY_REFUNDEDインボイスがある会員 |
| 期限超過 | 選択月にUNPAID/PAYMENT_PENDINGインボイスがある会員 |

---

## アクティブ判定ロジック

```javascript
isActiveAt(subscription, date):
  1. startDate が date 以前でなければ → 非アクティブ
  2. status === 'PAUSED' → 非アクティブ
  3. status === 'ACTIVE' → アクティブ
  4. status === 'CANCELED' or 'DEACTIVATED':
     - canceledDate が date より後 → アクティブ（解約前）
     - canceledDate が date 以前 → 非アクティブ
```

---

## プラン変更の検出ロジック

```
1. 全サブスクリプション（全店舗横断）から、顧客ごとの最も早いstartDateを特定
2. あるサブスクの startDate の月 ≠ 顧客の最初のstartDateの月 → プラン変更
3. 同じ月に複数のサブスクを開始した場合は、すべて「新規入会」としてカウント
   （ただし顧客ID重複排除により1人としてカウント）
```

---

## データソース

| API | 取得データ | 用途 |
|-----|-----------|------|
| Square Subscriptions API | サブスクリプション一覧（全ステータス） | 会員ステータス・入会日・解約日 |
| Square Catalog API | プラン詳細（名前・価格・周期） | プラン名・月額表示 |
| Square Invoices API | インボイス一覧（13ヶ月分） | 決済実績の確認・MRR計算 |
| Square Payments API | 決済一覧（13ヶ月分） | 総売上高の計算 |
| Square Refunds API | 返金一覧（13ヶ月分） | 返品額の計算 |
| Square Customers API | 顧客名 | 氏名表示 |

---

## テスト・0円プランの除外

以下の条件に該当するサブスクリプションは集計から除外:
- プラン名に「テスト」を含む
- 月額が0円かつPAIDインボイスが一度もない
- 重複サブスクリプションID

---

## 店舗別メトリクス

各店舗ごとに以下を独立計算:
- 会員数（月末アクティブ）
- MRR（当月PAIDインボイス合計）
- ARPU（MRR ÷ 会員数）
- 解約率（前月アクティブ → 当月非アクティブ、全店舗横断で判定）
- 新規入会数（プラン変更除外）
- 解約数
- 総売上高（Payments API - Refunds）

---

## 更新日

2026年2月17日
