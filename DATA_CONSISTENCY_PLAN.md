# データ整合性 設計（DATA_CONSISTENCY_PLAN）— Phase 1

作成日: 2026-09-17 / ブランチ: `feature/cc-foundation` / 状態: **設計。既存58箇所は一括変更しない**

> 目的: 「入力したのに消えた」をなくす。
> 現状で最も現実的な脅威は、外部攻撃ではなく **同時操作によるデータの上書き消失**。

---

## 1. いま何が起きているか（30秒で）

全社データは `api/plan-store.js` が1つのKVに **「JSONの塊（ブロブ）」** として保存している。
更新は「①全部読む → ②JSで書き換える → ③全部書き戻す」の3手順で、**この間に他の人が書くと消える**。

```
Aさん: 読む {投稿:[1,2,3]} ──編集──> 書く {投稿:[1,2,3,4]}
Bさん: 読む {投稿:[1,2,3]} ──編集──> 書く {投稿:[1,2,3,5]}   ← 4 が消える
```

この書き戻しが **58箇所**（本ブランチ追加分を除く）。
原子的に守られているのは**チャット送信のみ**（Upstash の Lua による追記）。
コード中のコメントにも「同時送信で消える不具合」への対処痕跡があり、**すでに実害として一度起きている**。

---

## 2. 正本（Source of Truth）の定義

「どのデータが正しいか」を先に決める。ここが曖昧だと、競合の解決方針も決まらない。

| データ | 正本 | Dashboard KV の位置づけ | 競合時 |
|---|---|---|---|
| 予約・来店・購入・顧客・**広告費** | **SalonOne API** | 表示用キャッシュのみ | 常に SalonOne を再取得して上書き。KV側は捨てて良い |
| 売上・課金・返金 | **Square API** | 表示用キャッシュのみ | 同上 |
| Meta広告の詳細Performance（Campaign/AdSet/Ad/Creative/CTR/CPC/CPM/Frequency） | **Meta Marketing API 経由の naoru-ai-platform** | 保持しない（都度取得） | Platform の値を採用 |
| AI分析・確定Metric | **naoru-ai-platform** | 保持しない | Platform の値を採用 |
| 社内コミュニケーション（チャット/掲示板/イベント/プレゼンス） | **Dashboard KV** | **ここが正本** | §4 の規則で解決 |
| 人事・社内制度（手当/サンクスギフト/アカウント/プロフィール） | **Dashboard KV** | **ここが正本** | §4 の規則で解決 |
| 手入力の運用値（広告費の手入力・除外リスト・パトロール設定） | **Dashboard KV** | **ここが正本**（ただし広告費は §2-1） | §4 の規則で解決 |
| 承認・監査・Agent実行ログ（本ブランチ追加） | **Dashboard KV**（将来 Platform Supabase） | **ここが正本** | 追記のみ＝競合しない |

### 2-1. 広告費の正本を SalonOne に確定（方針変更）

**変更前**: 画面によって「SalonOne `by-channel` の `ad_spend`」と「手入力 `naoru:adspend:v1`」が混在。
**変更後**: **SalonOne API の値を正式値とする。**

理由: Meta以外に HotPepper・チラシ・その他媒体があり、Meta Marketing API では全広告費を賄えない。
SalonOne は全媒体の `ad_spend` を持つため、ここを一本化しないと「全社広告費」が定義できない。

| 用途 | 採用する値 |
|---|---|
| 全社広告費・店舗別広告費・媒体別広告費・CPA/CAC/ROAS の分母 | **SalonOne `marketing/by-channel` の `ad_spend`** |
| SalonOne に入っていない媒体の補完 | 手入力 `naoru:adspend:v1`（**補完であることを画面に明示**） |
| Meta の Campaign/Creative 単位の按分 | Platform（Meta API）の比率 × SalonOne の実額 |

→ **Meta API と SalonOne の Meta広告費に差異があれば「広告費データ差異あり」を警告表示**（`KPI_UI_SPEC.md` §4）。
　 差異があっても **SalonOne 側を表示値として採用**し、差異は注記として出す（勝手に合算・平均しない）。

---

## 3. 危険度の優先順位（58箇所を一括で変えない）

危険度 = **書込頻度 × 同時に書く人数 × 失われたときの痛み**。

| 順位 | データ | 書戻し箇所 | 頻度 | 同時書き手 | 失うと | 危険度 |
|---|---|---|---|---|---|---|
| **1** | **掲示板 `board`** | **9** | 高 | **全社員** | 投稿・コメントが消える。**既読更新が投稿を巻き込む** | 🔴 最高 |
| **2** | **手当 `allowance`** | 3 | 締切前に集中 | 全セラピスト | **提出が消える＝返金明細に計上されない＝金銭影響** | 🔴 最高 |
| **3** | **イベント表 `events`** | 3 | 編集中は700msごと | 複数人が同時編集 | 入力中のセルが消える | 🔴 高 |
| **4** | **サンクスギフト `thanksgift`** | **7** | **毎月1〜2日に全社集中** | 全社員 | 投票が消える（再投票期間は2日しかない） | 🔴 高 |
| 5 | 広告費 `adspend` | 2 | 月数回 | 本部数名 | 全マーケKPIの分母が狂う | 🟠 中 |
| 6 | プロフィール `profile` | 5 | 低 | 各自 | 自分の編集が消える | 🟠 中 |
| 7 | プッシュ購読 `push` | 3 | 中 | 全社員 | 通知が届かなくなる | 🟠 中 |
| 8 | FAQ / knowledge / knowcand | 8 | 低 | 本部数名 | AI回答の根拠が欠ける | 🟡 低 |
| 9 | 除外リスト / accountmeta / patrol / zktherapist | 6 | 低 | 本部 | 再入力で回復 | 🟡 低 |
| 10 | `ailog` | 2 | 中 | 全社員 | ログの一部欠落（業務影響なし） | 🟢 極小 |
| 11 | `presence` | 2 | **最高（30秒毎・全員）** | 全社員 | **TTL75秒で自動回復** | 🟢 極小 |
| 12 | `sbcache` / `soflmap` / `meo` | 5 | 低（cron） | 1（cron） | 再取得で回復 | 🟢 極小 |
| — | チャット `chat` | 2 | 最高 | 全社員 | — | ✅ **対策済み** |

### 掲示板が最高リスクである理由（chat が既に踏んだ地雷と同じ）

`naoru:board:v1` は `{ posts:[...], reads:{staffId:ms} }` を **1つのブロブに同居**させている。
つまり **「誰かが掲示板を開いて既読になる」たびに、投稿配列を含む全体が書き戻される**。
閲覧者が多いほど、投稿・コメントを踏み潰す確率が上がる。

チャットは同じ問題に先に当たり、`naoru:chat:reads:v1` として**既読を別キーに分離**した
（コード中コメント: 「頻繁なread書込がmessagesを巻き込んで消す競合を防止」）。
**掲示板にはこの対策がまだ入っていない。**

---

## 4. 競合したときの扱い（データ種別ごとの規則）

「最後に書いた人が勝つ」を全部に適用しない。データの性質で分ける。

| 種別 | 規則 | 例 |
|---|---|---|
| **追記型**（増えるだけ） | **原子的追記**。競合の概念がない | 掲示板の投稿・コメント、チャット、監査ログ、Agentログ、ailog |
| **本人所有**（自分の行だけ書く） | **キーを人ごとに分ける**。他人の行に触れない | 既読、プレゼンス、プロフィール、プッシュ購読 |
| **共同編集**（同じ行を複数人が触る） | **楽観ロック**。競合したら 409 → 再読込してマージ → 再送 | イベント表、広告費、FAQ、承認の判断 |
| **1人1件の upsert** | **決定的ID**で上書き（＝冪等） | サンクスギフト投票（`対象月__投票者ID`）、手当提出（提出ID） |
| **キャッシュ**（正本が外部） | **競合を無視**。壊れたら捨てて再取得 | sbcache、meo、soflmap |

---

## 5. 原子的更新へ変える箇所と、その手段

### 5-1. 3つの手段

| 手段 | 実装 | 適する種別 |
|---|---|---|
| **A. キー分割** | 頻繁に書く部分を別キーへ出す | 本人所有（既読・プレゼンス） |
| **B. 原子的追記** | 既存の `kvAppendJson`（Upstash `EVAL` + Lua）を一般化 | 追記型 |
| **C. 楽観ロック** | ブロブに版（`_v` または `updatedAt`）を持たせ、不一致なら 409 | 共同編集 |

> ⚠️ **C は KV では完全ではない**。読み取りから書き込みまでの間に他者が書く窓は残る。
> ただし実務上の競合の大半は「BさんがAさんの判断前の画面を開いたまま操作した」ケースで、
> これは版の不一致として**確実に検出できる**。窓を完全に閉じるには Lua での compare-and-set（後述 §7）が要る。

### 5-2. 適用順（1PR＝1データ種別。まとめて出さない）

| # | 対象 | 手段 | 具体的な変更 | 期待効果 |
|---|---|---|---|---|
| 1 | **掲示板 既読** | A | `reads` を `naoru:board:reads:v1` へ分離（chatと同じ形） | **閲覧が投稿を消す経路が消滅**。最大の穴が塞がる |
| 2 | **掲示板 投稿・コメント** | B | `posts` を `kvAppendJson` で追記。コメントは投稿別キー `naoru:board:c:<postId>` へ | 同時投稿で消えない |
| 3 | **手当 提出** | B+冪等 | 提出は追記＋提出IDで重複排除。`productivity` は別キーへ分離 | 締切直前の同時提出で消えない・二重送信も安全 |
| 4 | **イベント表** | C | 行単位 `updatedAt` で楽観ロック。409 は自動再取得→マージ→再送（最大3回） | 同時セル編集で入力が消えない |
| 5 | **サンクスギフト** | A+B | `votes` を投票者別キーへ、`log` を追記型へ分離 | 月初2日の集中に耐える |
| 6 | **広告費** | C | rangeKey×店舗×媒体の粒度で楽観ロック | 金額の取り違えを検出 |
| 7 | **プロフィール / プッシュ購読** | A | 本人キーへ分離 | 他人の行を踏まない |
| 8 | 残り（FAQ/knowledge/patrol/accountmeta ほか） | C | 楽観ロックのみ | 低頻度なので最後 |

**1〜3 だけで、実害が出うる経路のほぼ全部が塞がる。** 4〜8 は順次。

### 5-3. 本ブランチで既に適用済み（新規データのみ）

| データ | 手段 | 実装 |
|---|---|---|
| 監査ログ `naoru:cc:audit:v1` | **B** 原子的追記 | `kvAppendJson`（KV以外はフォールバック） |
| Agent実行ログ `naoru:cc:agentlog:v1` | **B** 追記 ＋ 個別更新 | 開始は追記、終了は対象1件の更新 |
| 承認 `naoru:cc:approval:v1` | **B** 追記 ＋ **C** 楽観ロック | 作成は追記。判断は `expectedUpdatedAt` 不一致で **409 `stale`** |

> 承認センターでは、409 のときに画面へ
> 「他の人が先に処理しました。最新の内容を再読み込みしました。」と出して自動再取得する。
> **既存データの構造は一切変更していない。**

---

## 6. データ消失を防ぐ、コード以外の3つの備え

| 備え | 内容 | 現状 |
|---|---|---|
| **① 追記型の監査ログ** | 誰が何をしたかが残れば、消えても「何があったか」から復元できる | ✅ 本ブランチで基盤を追加 |
| **② スナップショット** | 確定した月次値を別キーに凍結し、現在値が壊れても比較・復元できる | ⚠️ `sbcache` のみ。全体へ拡張は未実施 |
| **③ バックアップ** | KVの定期エクスポート | ❌ **未実施。要検討**（§9） |

---

## 7. 版管理（optimistic locking）の具体形

### 7-1. ブロブに版を持たせる

```jsonc
{ "_v": 42, "_at": 1789656000000, "_by": "若林", "posts": [ /* … */ ] }
```

- GET のレスポンスに `_v` を含める
- POST は `expectedV` を必ず送る
- サーバー: `expectedV !== 現在の_v` なら **409** ＋ 現在値を返す
- クライアント: 409 を受けたら **再取得 → 自分の変更を当て直す → 再送**（最大3回、それでも失敗なら画面に明示）

### 7-2. 窓を完全に閉じる場合（将来・KVのみ）

Upstash の `EVAL` で compare-and-set を1往復にする:

```lua
-- KEYS[1]=キー / ARGV[1]=期待する版 / ARGV[2]=新しい値(JSON)
local raw = redis.call('GET', KEYS[1])
local curv = 0
if raw then curv = (cjson.decode(raw)._v or 0) end
if curv ~= tonumber(ARGV[1]) then return {err = 'conflict:' .. curv} end
redis.call('SET', KEYS[1], ARGV[2])
return 1
```

既存の `kvAppendJson` と同じ仕組み（`EVAL`）なので、**新しい依存は増えない**。
Supabase 版は `UPDATE … WHERE _v = $1`（行が0なら競合）で同じ意味になる。

### 7-3. 編集中はポーリング取り込みを止める

イベント表は既に `evEditingRef` で実装済み。**この作法を共同編集の全画面に広げる**
（掲示板のコメント入力中・広告費入力中）。楽観ロックと併用してはじめて「入力が消えない」体験になる。

---

## 8. 冪等性（idempotency）

「同じ操作が2回届いても1回分にしかならない」こと。ネットワーク再送・二重クリック・
モバイルの復帰リトライで**必ず起きる**ため、前提として設計する。

| 操作 | 冪等キー | 現状 |
|---|---|---|
| サンクスギフト投票 | `対象月__投票者ID` | ✅ 実装済み（upsert） |
| 手当の提出 | 提出 `id` | ✅ 実装済み（id で filter + push） |
| 掲示板 投稿 | **クライアント生成 `postId`** | ⚠️ **要追加**（現在サーバー側で採番＝再送で重複） |
| チャット送信 | メッセージ `id` | ✅ `genId` をクライアントで生成 |
| 承認の判断 | `approvalId` ＋ 状態機械 | ✅ 本ブランチ。終端状態への再遷移は拒否される |
| Agent実行 | `runId` ＋ 二重終了の拒否 | ✅ 本ブランチ（`already_finished`） |
| Meta への操作実行（将来） | `approvalId` を実行キーに使う | 📋 設計済み・未実装 |

**規則**: 書き込み系は**すべてクライアントが決定的IDを作って送る**。
サーバーはそのIDで「すでにあるなら何もしない」を返す。

---

## 9. 未解決（人の判断が必要）

| # | 論点 | なぜ決められないか | 誰に |
|---|---|---|---|
| 1 | KV のバックアップ | 現在バックアップが無い。Upstash の機能・費用・頻度が未確認 | 技術責任者 |
| 2 | 承認・監査ログを Platform Supabase へ移すか | Platform が Phase 0（未構築）。移すまでは KV が正本 | 技術責任者 |
| 3 | §5-2 の 1〜3 を実施してよいか | 既存データ構造の変更を伴う（移行スクリプトが必要） | 技術責任者 |
| 4 | 掲示板の既読分離時の移行 | 既存 `reads` を新キーへ写す間、両方を読む期間が必要 | 技術責任者 |
| 5 | SalonOne 広告費と手入力広告費の突合 | 過去分をどちらに寄せるか（数字が変わる月が出る） | マーケ責任者 |

---

## 10. 次のPR候補（この順に1つずつ）

```
feature/cc-board-reads-split     掲示板の既読を別キーへ（最大の穴。移行つき）
feature/cc-board-append          掲示板の投稿・コメントを原子的追記へ
feature/cc-allowance-append      手当の提出を追記＋冪等へ（金銭影響）
feature/cc-optimistic-events     イベント表に楽観ロック＋409リトライ
feature/cc-kv-cas                Lua compare-and-set の共通ヘルパー（lib/store/）
```

各PRの条件は `INTEGRATION_PLAN.md` §I-3（1PR＝1機能＝1フラグ＝ロールバック可能）に従う。
