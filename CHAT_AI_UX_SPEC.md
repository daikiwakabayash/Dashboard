# CHAT_AI_UX_SPEC

Chat 内 AI（@AI メンション / AI Question Mode / 透明性 / Feedback）の UX 仕様。

---

## 1. 3つの会話モードの区別

| モード | 場所 | 見える人 | 用途 | 視覚的な区別 |
|--------|------|----------|------|--------------|
| **Private AI Chat** | 「AIに質問」タブ（`askai`） | **自分だけ** | 周囲に見せず AI に相談 | 画面全体が AI 配色（indigo）・ヘッダーに「🔒 自分だけが見られます」 |
| **Group Chat + @AI** | 通常の Room 内 | Room メンバー全員 | AI にも人にも見える質問 | AI 吹き出しは indigo 枠＋🤖バッジ。質問側にも「@AI」チップ |
| **通常 Group Chat** | 通常の Room 内 | Room メンバー全員 | 人間同士の会話 | 既存デザインのまま（変更なし） |

- Private AI Chat の履歴は端末ローカル（`naoru_askai_v1`・アカウント別）。共有ストアには入れない。
- Group Chat の @AI 応答は通常メッセージとして保存され、**後から誰でも読み返せる**（ナレッジになる）。

---

## 2. @AI メンション

### 起動
- 本文に `@AI` を含めて送信（半角 `@` / 全角 `＠` 両対応・既存 `[@＠]\s*ai\b` を踏襲）
- または「AIに質問」ボタン（§3）

### 挙動
1. 質問はまず**通常メッセージとして投稿**される（誰が何を聞いたかが Room に残る）。
2. AI が同じ Room に `fromStaffId='__ai__'` / 名前「🤖 NAORUアシスタント」で返信。
3. 人間のメンバーも質問・回答の両方を読める。返信・リアクションも通常どおり可能。
4. AI が確信を持てない場合は `NEEDS_HQ` → 本部メンションでエスカレ（既存挙動を維持）。

### 例
```
田中（NAORU渋谷院）
@AI 家族施術制度のルール教えて

🤖 NAORUアシスタント                        ← indigo 枠
家族施術制度は …（回答本文）…

📚 出典: 福利厚生規程（2026/09/12 更新）      ← §4 透明性ブロック
🎯 確信度: 高
[ 本部に確認する ]  [ 👍 ] [ 👎 ] [ ✏️ 修正 ]
```

---

## 3. AI Question Mode（「AIに質問」ボタン）

既存ボタンは**残す**。ただしトグル式の「モード」に変更する。

| 状態 | 入力欄の見た目 | 送信ボタン | プレースホルダ |
|------|----------------|-----------|----------------|
| 通常 | 既存（白背景） | 「送信」 | 「メッセージを入力」 |
| **AI Mode** | 入力欄が indigo 枠 + 上部に「🤖 AIに質問中（この内容は @AI 宛に送信されます）」バー | 「🤖 AIに質問」 | 「AIへの質問を入力」 |

- ボタンをもう一度押すと通常モードへ戻る。ESC でも戻る。
- AI Mode で送信すると、本文の先頭に `@AI ` が自動付与される（Room には `@AI` 付きで残る＝後から見ても AI 宛だと分かる）。
- **通常 Send と AI Send を取り違えないこと**が最優先。モード中は入力欄・送信ボタン・上部バーの 3 箇所で同時に示す。

---

## 4. AI 回答の透明性

AI 回答には可能な限り以下を添える（取得できないものは非表示にし、**推測で埋めない**）。

| 項目 | データ源 | 表示 |
|------|----------|------|
| AI 回答である表示 | 固定 | 🤖 バッジ + indigo 枠 + 「AIによる回答」 |
| 参照 Source | FAQ / ナレッジ資料の `id` + `title` | 「📚 出典: 福利厚生規程」（クリックで該当 FAQ を表示） |
| 情報更新日時 | FAQ / 資料の `updatedAt` | 「2026/09/12 更新」 |
| Confidence | モデル出力 + 参照ヒット数から算出 | 高 / 中 / 低（数値は内部保持） |
| 本部に確認する | ボタン | 押すと本部へメンション付きで転送（`ai.escalate.receive` 保持者） |

メッセージに保存する形（既存 message に追加・optional）:
```jsonc
"ai": {
  "used": true,
  "agent": "faq",
  "sources": [{ "kind": "faq|knowledge", "id": "faq_123", "title": "福利厚生規程", "updatedAt": "2026-09-12T..." }],
  "confidence": 0.82,
  "level": "high|mid|low",
  "escalated": false,
  "answeredAt": "2026-09-17T..."
}
```

### 断定しない条件（エスカレ）
- 参照ソースが 0 件
- Confidence が閾値未満（既定 0.5）
- 金銭・雇用・労務・医療広告・法令に関わる話題（キーワード設定で判定）
- 数値を問われたが店舗スナップショットに該当データが無い

このとき AI は回答を断定せず:
> この内容は本部確認が必要です。本部へ確認を依頼しました。

と表示し、本部メンバーをメンションする（既存 `NEEDS_HQ` 挙動を踏襲・表示だけ強化）。

---

## 5. Recipient Preview UI

`CHAT_RECIPIENT_RESOLVER.md` §8 のレイアウトをモーダルで表示する。

- 入力欄の下に「宛先チップ」を表示（`新宿 ×` `渋谷 ×` `大阪(除外) ×`）。チップは人間が直接削除できる。
- `[送信]` を押すまで**一切送信されない**。
- 大量送信時（既定 50 Room / 300 名超）は警告を赤地で強調し、チェックボックス同意 → `[送信]` の二段階。
- 送信後は「92グループへ送信しました」のトーストと、監査 ID（`audit_xxx`）を控えめに表示。

---

## 6. AI Feedback（👍 / 👎 / 修正）

| 権限 | 👍 | 👎 | ✏️ 修正 |
|------|----|----|---------|
| root / hq | ✅ | ✅ | ✅ |
| owner / manager | ✅ | ✅ | ✅ |
| staff | ✅ | ✅ | ❌ |

- 👎 を押すと理由の選択（事実誤り / 古い情報 / 的外れ / 言い回し）＋自由記述。
- 「✏️ 修正」は正しい回答を入力 → **Knowledge 候補**（`?type=knowcand`）へ送る。
  既存の「🤖 この回答をFAQに追加」導線と統合する。
- 保存先: `?type=aifeedback` → `naoru:chat:aifeedback:v1`
```jsonc
{ "id":"fb_x","tenantId":"default","messageId":"m_x","roomId":"store_s_1",
  "question":"…","answer":"…","verdict":"up|down|fix","reasons":["stale"],
  "correction":"…","sources":[…],"byStaffId":"staff_1","byRole":"hq","createdAt":"…" }
```
- **同じ誤回答を繰り返さないため**、次回以降の回答生成時に「同一質問に対する `fix` フィードバック」を
  プロンプトへ優先注入する（FAQ 化されるまでの暫定補正）。

---

## 7. AI と権限

- AI は `role: 'agent'`。**人間の権限を超える Recipient を作れない**（`CHAT_PERMISSION_MATRIX.md` §1）。
- AI は送信を実行しない。Preview を作るだけ。
- AI は承認者になれない（監査の `approved_by` に `__ai__` は入らない）。
- AI が参照できるデータも actor のスコープに限定する（staff の @AI は自店データのみ）。

---

## 8. アクセシビリティ / モバイル

- AI Mode のバーは色だけでなく**テキストとアイコン**でも状態を示す（色覚多様性対応）。
- モバイルでは Preview をフルスクリーンシートで表示し、`[送信]` は親指の届く下部に固定。
- 読み上げ対応: AI メッセージに `aria-label="AIによる回答"` を付与。

---

## 9. テスト要件

- `tests/chat-ai-ux.test.js`（ロジック部分）
  - `@AI` トリガ判定（半角/全角/大文字小文字/単語境界）
  - AI Mode の本文に `@AI` が一度だけ付与される（二重付与しない）
  - Confidence → level（高/中/低）の境界
  - エスカレ条件（ソース0件 / 低 Confidence / センシティブ語）
  - Feedback レコードの生成・権限（staff は fix 不可）
- `tests/html-structure.test.js` に AI Mode の UI 要素が存在することを追加
