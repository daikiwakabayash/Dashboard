# KPI 定義・UI 仕様（KPI_UI_SPEC）

作成日: 2026-09-17 / ブランチ: `feature/cc-foundation` / 状態: **設計**
関連: `MARKETING_DASHBOARD_PLAN.md`（画面構成）／`REPORT_GENERATOR_UI.md`（資料生成）

> この文書の目的は「**この数字はどこから来て、どう計算され、どれくらい信用できるか**」を
> 画面の上で必ず答えられるようにすること。

---

## 1. KPIカードの構造（全KPI共通）

すべてのKPIカードは **同じ4段構造**を持つ。ここを共通コンポーネント `KpiCard` にする。

```
┌──────────────────────────────────┐
│ ① ラベル                      ⓘ │  ← ⓘ は既存 InfoTip（定義・計算式・対象期間）
│ ② ¥58,420                       │  ← 値（tabular-nums・欠損は「—」）
│ ③ 前月比 ▲12.4%  前年比 ▼3.1%   │  ← 比較（改善=緑 / 悪化=赤 は指標ごとに向きを定義）
│ ─────────────────────────────── │
│ ④ Source  Square + SalonOne     │  ← 出所
│    Updated 2026/09/17 22:00     │  ← 最終更新
│    Quality ● Verified           │  ← 品質
└──────────────────────────────────┘
```

### ④ をたたむ / 開く

常時4段だと情報が多いため、既定は **④を1行に圧縮**（`Square+SalonOne・22:00・● Verified`）。
クリックで展開して全文・突合結果・計算式を出す。**非表示にはしない**（出所の無い数字を作らない）。

---

## 2. KPI 定義表

「向き」= 上がると良いか悪いか（色の向き）。`↑良` は増加を緑、`↓良` は減少を緑で出す。

| KPI | 計算式 | 正式データソース | 向き | 備考 |
|---|---|---|---|---|
| **広告費** | Σ `by-channel.ad_spend` | **SalonOne** | — | 全媒体。Meta API は使わない |
| **予約数** | 期間内に `received_at` を持つ新規客数 | **SalonOne** | ↑良 | 受付日コホート |
| **来店数** | 上記のうち `first_appointment_status ∈ {completed, visited}` | **SalonOne** | ↑良 | — |
| **購入数** | 上記のうち `joined = true` | **SalonOne** | ↑良 | 入会 |
| **CPA** | 広告費 ÷ **予約数** | SalonOne | ↓良 | 「予約1件あたり」 |
| **CPO** | 広告費 ÷ **購入数** | SalonOne | ↓良 | 「購入(入会)1件あたり」 |
| **CAC** | (広告費 ＋ 獲得関連費) ÷ **購入数** | SalonOne (+財務) | ↓良 | **§3 の注意を必ず読むこと** |
| **CVR（来店）** | 来店数 ÷ 予約数 | SalonOne | ↑良 | — |
| **CVR（入会）** | 購入数 ÷ 来店数 | SalonOne | ↑良 | — |
| **CVR（総）** | 購入数 ÷ 予約数 | SalonOne | ↑良 | 来店CVR × 入会CVR |
| **ARPU** | 期間売上 ÷ 課金顧客数 | **Square** | ↑良 | 月次。顧客数は期間中に課金のあった人 |
| **Actual LTV** | コホートの累計売上 ÷ コホート人数 | **Square + SalonOne** | ↑良 | **実績のみ。予測を混ぜない** |
| **Expected LTV** | ARPU × 予測継続月数 | Square + SalonOne | ↑良 | 継続率カーブ（既存 `lib/cohort.js`）から |
| **LTV/CAC** | Expected LTV ÷ CAC | 算出 | ↑良 | 一般的な健全基準は 3.0x 以上（§6-3） |
| **CAC Payback** | CAC ÷ 月次ARPU（粗利ベース） | 算出 | ↓良 | 回収までの**月数**。粗利率未取得なら §3 |
| **ROAS** | 売上 ÷ 広告費 | Square + SalonOne | ↑良 | 倍率表示（3.19x） |

### 2-1. CPA / CPO / CAC を分けて持つ理由

現在のダッシュボードは「CPA」という語を**来店単価**の意味で使っている箇所がある
（`cpaAlerts` は `広告費 ÷ 新規数`、店舗別テーブルは `広告費 ÷ 来店数`）。
**同じ名前で違う数字**が出ると会話が壊れるため、本仕様では明確に分ける。

```
CPA … 予約1件あたり   （広告費 ÷ 予約数）
CPV … 来店1件あたり   （広告費 ÷ 来店数）← 既存「CPA(来店)」はこれ
CPO … 購入1件あたり   （広告費 ÷ 購入数）
CAC … 顧客獲得単価    （獲得関連費込み ÷ 購入数）
```

⚠️ **移行時の注意**: 既存画面の「CPA」表記は当面変えない（利用者の記憶と食い違うため）。
新画面では上記の4語を使い、`InfoTip` に「旧画面の『CPA(来店)』は本画面の CPV です」と明記する。

---

## 3. 「計算できないKPI」の扱い（最重要）

**欠損をゼロにしない。推定で埋めない。** 計算できないときは計算できないと表示する。

| 状況 | 表示 | Quality |
|---|---|---|
| 獲得関連費（人件費・インセンティブ）が未取得 | **CAC は「広告費のみのCAC」として表示**し、ラベルに `(広告費のみ)` を付ける | `Partial` |
| 粗利率が未取得 | **CAC Payback を出さない**。「粗利率が未設定のため算出できません」 | `Missing` |
| 広告費が未入力の店舗 | CPA/CPO/CAC/ROAS は `—`。**合計からも除外**し、注記に「N店を除外」 | `Missing` |
| コホート期間が足りない（加入3ヶ月未満） | Expected LTV は `—`。Actual LTV のみ表示 | `Partial` |
| Platform 未接続 | Meta階層・AI考察は「未接続」。**上位KPIは通常表示** | `Missing`（該当項目のみ） |
| 当月（進行中） | 値の横に `進行中` バッジ。前月比は日割り換算であることを明記 | `Provisional` |

---

## 4. データ品質ステータス（Quality）

| Status | 色 | 意味 | 条件 |
|---|---|---|---|
| ● **Verified** | 緑 | 正式ソースから取得し、突合も通った | 正式ソース取得済み ＋ 差異チェック合格 ＋ 鮮度が基準内 |
| ● **Provisional** | 青 | 期間が締まっていない（進行中の月） | 当月、または SalonOne の確定前（翌月3日より前） |
| ● **Partial** | 黄 | 一部の入力が欠けたまま算出した | 一部店舗の広告費未入力、CACの費目不足 など |
| ● **Estimated** | 黄 | 按分・推定を含む | 施策リンク別の広告費按分、Expected LTV など |
| ● **Stale** | 橙 | 取得はできたが古い | 最終更新が基準（既定24時間）を超過 |
| ● **Mismatch** | 赤 | 複数ソースの値が食い違う | §5 の差異判定に該当 |
| ● **Missing** | 灰 | 算出不可 | 必要なデータが無い |

**規則**: 1つのKPIが複数に当てはまるときは **最も悪いものを表示**する
（Verified < Provisional < Partial = Estimated < Stale < Mismatch < Missing の順で悪い）。

新規モジュール `lib/dataquality.js` に集約し、テストを書く:

```js
qualityOf({ sources, updatedAt, now, missingInputs, estimated, mismatch, provisional })
  → { status: 'Partial', reasons: ['3店舗の広告費が未入力'], worst: 'Partial' }
```

---

## 5. 広告費データ差異の警告

### 判定

```
diffRate = |metaApiSpend − salonOneMetaSpend| ÷ salonOneMetaSpend
tolerance = 0.05（既定・本部が変更可）
diffRate > tolerance → Mismatch
```

### 表示

```
┌──────────────────────────────────────────────┐
│ 広告費                                    ⓘ │
│ ¥1,940,000                                   │
│ 前月比 ▲8.2%                                 │
│ ───────────────────────────────────────────  │
│ ⚠ 広告費データ差異あり                        │  ← 赤バー・クリックで展開
│ Source  SalonOne（正式値）                    │
│ Updated 2026/09/17 05:30                     │
│ Quality ● Mismatch                           │
└──────────────────────────────────────────────┘
        ▼ 展開
┌──────────────────────────────────────────────┐
│ Meta媒体の広告費に差異があります               │
│   SalonOne（正式値）   ¥1,180,000  05:30取得  │
│   Meta Marketing API   ¥1,194,300  13:00取得  │
│   差額 ¥14,300（+1.21%）                      │
│                                              │
│ 表示している金額は SalonOne の値です。         │
│ Meta APIの値で上書きは行いません。             │
│ 考えられる原因: 計上タイミングのずれ／通貨換算  │
│ ／アカウントと店舗の対応漏れ                   │
│                          [ 差異の詳細を見る ] │
└──────────────────────────────────────────────┘
```

### 連動

- `withinTolerance=false` → アラート基盤へ `ad_spend_mismatch` を起票
  （`dedupeKey = ad_spend_mismatch|<店舗>|<年月>` で同じ月に何度も鳴らさない）
- 資料生成（`REPORT_GENERATOR_UI.md`）では **Mismatch を含む期間は注記が必須**

---

## 6. InfoTip（ⓘ）に必ず書く4項目

既存の `InfoTip` コンポーネントをそのまま使う。**この4つを省略しない。**

```
① 定義      この数字が何を表すか（1行）
② 計算式    分子 ÷ 分母（実際の値も入れる）
③ 対象期間  受付日コホートか、売上計上日か、を明示
④ データ源  SalonOne / Square / Platform のどれか
```

記入例（CAC）:

```
顧客獲得単価。1人の購入者を獲得するのにかかった費用。
計算式: 広告費 ¥1,940,000 ÷ 購入数 241件 = ¥8,050
対象期間: 2026年8月に受付(received_at)のあった新規客（来店・入会が翌月でも含む）
データ源: SalonOne marketing/by-channel（広告費）＋ marketing/new-customers（購入数）
※ 獲得関連の人件費は未取得のため、広告費のみのCACです（Quality: Partial）
```

### 6-3. 基準値の表示

比較対象がないと数字は読めないため、以下は**基準線を併記**する。

| KPI | 基準 | 出所 |
|---|---|---|
| LTV/CAC | 3.0x | 一般的なユニットエコノミクスの健全基準。**NAORUの基準は未確定（§8）** |
| CPA | ¥5,000 | 既存コード `CPA_ALERT_THRESHOLD`。**店舗・媒体別にするか未確定** |
| 来店CVR | 全社平均 | 自社データ（期間内の全社値） |
| ROAS | 全社平均 | 自社データ |

⚠️ 外部のベンチマークを断定的に出さない。自社平均か、出所の明記できる基準のみ。

---

## 7. 比較（前月比・前年比）の規則

| 項目 | 規則 |
|---|---|
| 前月比 | 同じ日数で比較。当月（進行中）は**日割り換算**し、`日割り` バッジを付ける |
| 前年比 | 同月同士。**店舗数が変わっている場合は「既存店のみ」も併記**（新規出店で増えたのか、既存が伸びたのかを分ける） |
| 色 | 指標ごとの「向き」に従う（CPA/CPO/CAC/Payback は減少が緑） |
| 欠損 | 比較対象の期間にデータが無ければ `—`。**0%と表示しない** |
| 表示 | `▲8.2%` / `▼3.6%` ＋ 実数の差分をツールチップで |

---

## 8. 実装メモ

### 新規モジュール（すべて `lib/` ＋ テスト）

| ファイル | 役割 |
|---|---|
| `lib/channelgroup.js` | `visit_source_name` → Meta/Google/HotPepper/Flyer/その他 |
| `lib/unitecon.js` | CPA/CPV/CPO/CAC/ARPU/LTV/LTV-CAC/Payback/ROAS の算出（**データ源非依存**＝入力を渡す設計。既存 `lib/cohort.js` と同じ思想） |
| `lib/dataquality.js` | Quality の判定と「最も悪いものを採用」 |
| `lib/reconcile.js` | SalonOne と Meta API の突合・差異率・按分 |

**既存 `lib/cohort.js` `lib/plan-calc.js` は流用する**（LTV・継続率の計算式を二重に持たない）。

### 未確定（人の判断が必要）

| # | 論点 | 誰に |
|---|---|---|
| 1 | CAC に含める費目（広告費のみ / 人件費・インセンティブ込み） | 経営・財務 |
| 2 | 粗利率の取得元（CAC Payback に必須） | 財務 |
| 3 | LTV/CAC の自社目標倍率 | 経営 |
| 4 | CPA 閾値を店舗・媒体別にするか | マーケ責任者 |
| 5 | 広告費差異の許容率（既定5%でよいか） | マーケ責任者 |
| 6 | 「既存店のみ」の定義（何ヶ月以上営業している店か） | 経営 |
