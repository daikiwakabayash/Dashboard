# 開発の引き継ぎ（Dashboard ①）

このファイルは、セッションが切れても次の担当が**ここだけ読めば再開できる**ようにするための記録です。
決定事項・再開地点・私（AI側）の判断待ちを置きます。日々の仕様は `CLAUDE.md` が正本です。

最終更新: 2026-09-18（ニュース試作V3・勉強会イベント・Creative の確認項目まで本番反映） / 記録者: Claude Opus 5（①統括窓口）

---

## 0. 体制と進め方（オーナー合意・2026-09-18）

- **オーナー**＝何を実現するか・優先順位・完成画面の確認・承認
- **①（このセッション）**＝引き継ぎ、作業分担、実装、テスト、実ブラウザ確認、レビュー、統合、本番反映、進捗管理
- 通常の調査・実装・テスト・修正・専用ブランチ保存・PR作成は、**毎回の確認なしで進める**
- 主作業1つ＋外部待ちの間に進める作業1つ。承認/認証待ちの項目だけ保留し、依存しない作業は続ける

### 私（AI側）が単独で行わないこと
新規課金、費用上限の拡大、未承認の広告操作、SNS公開、大量送信、印刷発注、データ削除、**公開範囲の拡大**。
認証・権限の回避や安全設定の一括無効化、本番への障害注入・大量負荷試験、検証データの正式集計への混入も行いません。

### 報告の型
「何が使えるようになったか／開くURL・メニュー・試し方／実データかデモか・ローカルか本番か／採用commit・残件／次に進める作業／オーナーの操作や判断が必要なこと」。
**コード保存・デモでの確認・本番での確認は必ず分けて書く**こと。

---

## 1. 正本（どこが真実か）

| 区分 | 正本 |
|---|---|
| 予約・来店・購入・店舗情報・正式広告費 | **SalonOne** |
| 店舗横断の計算・検索・AI・生成・実行管理 | **Platform**（naoru-ai-platform） |
| 表示・操作・確認・承認 | **Dashboard**（このリポジトリ） |

- 集計期間・基準日・タイムゾーン・指標定義は **Platform に集約**し、Dashboard で別計算を作らない
- 回答・分析には出典／対象期間／更新日時を付ける。**未取得・不明を 0 で埋めない**
- GitHub はコード・仕様・開発記録の保存先。業務データや秘密情報は置かない

---

## 2. いまの状態（2026-09-18 時点）

### 本番に入っているもの（main）

| 内容 | PR | 状態 |
|---|---|---|
| @AI チャットの同時実行対応（重複回答・回答消失の防止） | #400 | 本番反映済み |
| オーナー設定の「@AI 検証」ON/OFF スイッチ | #402 | 本番反映済み |
| @AI 検証の設定で選択肢が空になる不具合 | #403 | 本番反映済み |
| チャットのメンション候補に AI を追加 | #404 | 本番反映済み |
| 「AIに質問」ボタンの削除（メンションに統合） | #405 | 本番反映済み |
| ナレッジ資料の Google 自動更新（1日1回・更新は本部が確認） | #406 | コード反映済み。**下の「オーナー作業待ち」が未了のため実動作は未確認** |
| GAS 認証強化（別セッション） | `e021edf` / `83d5b84` | 本番反映済み |
| 上記のテスト追従（main が赤だったのを修復） | #408 | 本番反映済み |
| チャット: エリアで宛先をしぼる／未読の人にもう一度送る | #407 | 本番反映済み |
| チャット: 文章で宛先を指定する（店舗名・人名から割り出す） | #412 | 本番反映済み |
| ナレッジ取得の失敗理由を画面に出す／GAS の設定補助関数 | #410 / #411 | 本番反映済み |
| **Creative Library（暗号化保存＋認証付き配信・非同期生成）** | **#401** | **本番反映済み。ただしフラグ `cc_creative_library` は OFF のまま** |
| ニュース一覧を試作V3の見た目に（ヒーロー・ピックアップ・カード3列） | #418 | 本番反映済み |
| 勉強会・イベント（参加・定員・キャンセル待ち・チャット連携・ICS） | #419 | 本番反映済み |
| Creative の素材区分・利用許可・承認前の確認・制作費 ／ ROADMAP の対応付け | #420 | 本番反映済み |

### UI試作V3（ニュース・組織図）の進み具合

| | 内容 | PR | 状態 |
|---|---|---|---|
| A | 「重要掲示板／お知らせ」→「ニュース」 | #413 | ✅ 本番反映済み |
| F | 記事のイベントリンク→グループチャット参加 | #413 | ✅ 本番反映済み |
| D | 閲覧・リアクション状況一覧（既読／未読／確認しました／リアクション） | #414 | ✅ 本番反映済み |
| E | 組織図の自己紹介項目＋プロフィール権限の修正 | #415 | ✅ 本番反映済み |
| C-1 | 投稿フォーム拡張（カテゴリー／公開対象／確認要否／期限／ピックアップ／表紙） | #416 | ✅ 本番反映済み |
| B | 配色・活字・余白・ポップアップ（この2画面だけ） | #417 | ✅ 本番反映済み |
| C-2 | 下書き→プレビュー、過去記事の年月表示／追加読込 | — | 未着手（「保存済み」は #418 で実装） |
| G | ニュース一覧を試作V3の見た目に（ヒーロー・ピックアップ・カード3列・☆保存） | #418 | ✅ 本番反映済み |
| H | 勉強会・イベント（「集まる、広がる。」・参加・定員・過去の開催） | #419 | ✅ 本番反映済み |

⚠️ 内部IDは変えていない（`board` / `?type=board` / `naoru:board:v1`）。既存の投稿・コメント・既読・リアクション・プロフィールはそのまま。
⚠️ チャットの公開範囲は変えていない（本部/root限定）。オーナー指示により認可変更は不要。

**この過程で直した権限の問題（2件）**
1. プロフィールの保存・削除・非表示が `body.root` などクライアントの申告を信じていた → サーバーが確かめた本人で判定（#415）
2. ニュースの状況一覧は、氏名の一覧をサーバー側でも本部の管理者・投稿者に限定（#414）

### 開いている PR

| PR | 内容 | 次にすること |
|---|---|---|
| #399 | Meta 受入の不合格5件修正（③が 17/17 合格と報告） | オーナー承認待ち。**merge しない** |
| #397 | Meta freshness / 外貨 unit | #399 に内包（`#399 ⊃ #398 ⊃ #397`） |
| #395 | アクセスログ保全の依頼書（文書のみ） | 管理者の確認待ち |
| #391 / #384 / #383 | ②のチャット契約・段階公開 | ①で内容を精査して取り込み判断 |

### 私（①）が確認できていない場所

- **③のローカル保存先** `/Users/daiki/Documents/Codex/...` は**オーナーのMac上**で、私からは開けません。
  GitHub 側は `naoru-ai-platform` の `codex/phase1-knowledge-base`（`c6a9239`）に
  `CREATIVE_JOB_CONTRACT.md` / `creative_job_app.py` / `creative_jobs.py` / `creative_render.py` があると報告を受けています。
  **画像9点・18秒動画の実ファイルが GitHub にあるかは未確認**（zip で渡されたとの報告）。
- 他の Claude / Codex セッションを私から操作・停止・連携することはできません。**自動で連携できたとは報告しません。**

---

## 3. オーナー作業待ち（これが済むまで先へ進めない）

### A. ナレッジ自動更新（#406）を実際に動かす

1. **Apps Script**: `gas-knowledge.gs` の中身を、いま使っているプロジェクト（`PLAN_GAS_URL` / `SETTLEMENT_GAS_URL` の接続先）に追加
2. 「実行する関数」で **`setupKnowledgeSecret`** を選んで実行 → 実行ログに出た48文字をコピー
   （合言葉はこの操作で自動生成・自動保存されます。手で考える必要はありません）
3. 「デプロイを管理」→ 既存ウェブアプリを編集 → **新バージョン**でデプロイ（URLは変わらない）
4. **Vercel**（`dashboard` プロジェクト・Production）に、2でコピーした値を `KNOWLEDGE_GAS_SECRET` として登録。
   別途 `CRON_SECRET`（**別の値**）も登録
5. Vercel を再デプロイ（環境変数は次のデプロイから効く）
6. 動作確認: Apps Script で `testReadKnowledgeDoc`（FILE_ID を入れてから）→ Dashboard で「🔄 いま取り直す」

確認方法: FAQ管理（AI）→ ナレッジ資料 →「🔄 いま取り直す」。
「最終更新確認」の時刻が更新されれば動いています。止まっていれば理由が画面に出ます。

### B. Meta 認証（#399 の先）
`act_1335477837049931` の認証が整うまで、Meta 以外の開発を続けます。

### D. Creative Library を実際に使えるようにする（#401）

本番にコードは入っていますが、**フラグは OFF**、素材の保存鍵も未設定なので、まだ誰にも見えていません。
使い始めるには、Vercel（`dashboard` プロジェクト・Production）に次を登録してください。

| 名前 | 値 | 無いとどうなるか |
|---|---|---|
| `CREATIVE_ASSET_KEY` | **32文字以上のランダム文字列**（例: `openssl rand -base64 48`） | 素材を登録できない（画面に理由が出ます） |
| `CREATIVE_GEN_API_BASE` | ③の生成APIの接続先 | 「未接続」と表示。サンプルは作りません |
| `CREATIVE_GEN_API_KEY` | ③の生成APIの認証キー | 同上 |

⚠️ `CREATIVE_ASSET_KEY` は**素材を復号するための鍵のもと**です。**変更・削除すると、それまでに登録した素材が開けなくなります。**

登録して再デプロイしたあと、オーナー設定でフラグ `cc_creative_library` を ON にすると、
本部/root に「クリエイティブ」メニューが出ます。
**フラグを ON にするのは公開範囲の拡大にあたるため、私（AI側）からは行いません。**

### C. チャットの公開範囲
現在 **本部/root 限定**（`api/plan-store.js` の `chatTypes` ゲート）。
スタッフ・オーナーへ広げるのは**別途承認**が必要です。

---

## 4. 直近の完成目標（オーナー指定の順）

1. **本部で使えるチャット・@AI** — 質問→回答→履歴→出典→本部確認・訂正。
   続いて宛先指定・送信前確認・個別DM・予約送信・未読者への再通知（#407 で宛先指定と未読再通知を実装済み）
2. **Creative Library と実際の制作物** — 素材登録→生成依頼→画像表示・動画再生→複数案比較→修正依頼→新版確認→承認→完成ファイル取得
3. **Meta 読取と経営・集客分析** — 認証後
4. **承認付きの広告操作 → 限定自動運用**

### #401（Creative Library）の必須4点 — ✅ すべて解消（本番マージ済み）

| | 内容 | どう直したか |
|---|---|---|
| 1 | 素材の真正な非公開保存 | **保存先には暗号文しか置かない**。鍵は親鍵 `CREATIVE_ASSET_KEY` で包んで保存し、①の認証付き配信口だけがバイトを返す。`lib/creative-crypto.js`（1MiB区切り AES-256-GCM・Range対応・改ざん検知） |
| 2 | 修正依頼を対象版・元素材とともに③へ | `parent_creative_id` / `source_creative_version` / `revision_instructions` / `text_changes` / `source_asset_ids` を送る。受付は202で即返し、`job_status` で聞く |
| 3 | ID指定による上書き防止 | `asset_create` / `creative_create` の **IDはサーバー発番のみ**。クライアントの `id` を採用しない |
| 4 | 表示確認の分離 | `tests/creative-screen.test.js` は記述確認だけと明記。表示・再生は `scripts/creative-screen-check.mjs`（実ブラウザ・**18/18**） |

詳細は `CREATIVE_LIBRARY_CONTRACT.md`。

**確認できたこと（デモ・ローカル実ブラウザ＋スタブAPI）**
画像が実際に開ける（320x320）／動画が実際に再生できる（`readyState=4`・0.00→1.50s）／
完成ファイルを実際に保存できる（7387 byte）／保存先には暗号文しか無い（7415・37432 byte）。

**確認できていないこと（本番）**
`cc_creative_library` は OFF のまま。③の生成APIも未接続なので、**本番での実生成は未確認**です。

---

## 5. 確認のやり方（この3つを混同しない）

| 種別 | 意味 | 例 |
|---|---|---|
| **コード保存** | ブランチ・PRに入っただけ | 単体テスト |
| **デモでの確認** | ローカルの実画面＋スタブAPI。本番にはつながない | `scripts/*-screen-check.mjs`（headless Chromium） |
| **本番での確認** | デプロイ済みの本番URLで実際に動かす | 画面の時刻・実データ |

HTML の文字列を読むだけのテスト（`*-screen.test.js`）は**実表示の確認ではありません**。必ず分けて報告します。

実ブラウザ確認の動かし方:

```
DASHBOARD_ROOT=<repo>/public CDN_DIR=/tmp/cdn \
CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome \
PLAYWRIGHT_PATH=<repo>/node_modules/playwright/index.mjs \
node scripts/audience-screen-check.mjs
```

`/tmp/cdn` には react / react-dom の UMD を置く（無ければ cdnjs から取得）。

---

## 6. 再開地点

- **次にやること**: 経営ホーム（重要指標・要確認店舗・承認待ち・AIへの質問）
  → ニュース C-2（下書き→プレビュー・過去記事の年月／追加読込）
  → イベントの通知・過去の開催のふりかえり（**送る範囲のご指示待ち**）
- **要件の対応付け**: `ROADMAP.md`（①側の台帳。8人の業務AIと後続要件9件を段階へ）
- **外部待ち**: 上記「オーナー作業待ち」A・B・C・D
- **触ってはいけないもの**: 他担当の未マージブランチ（`feature/*`）、`naoru-ai-platform` への書き込み
